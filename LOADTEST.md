# Locust 부하 테스트

대용량 트래픽을 시뮬레이션하고 CloudWatch·Sentry·로그와 함께 관찰하기 위한 가이드.

## 1. 설치 (로컬 PC, 서버와 다른 머신 권장)

```bash
pip install locust
```

프로젝트 가상환경에 넣거나, 별도 venv 사용 가능.

## 2. 실행

프로젝트 루트에서:

```bash
# EC2 서버 대상 (기본)
locust -f locustfile.py --host=http://3.37.203.226:8000

# 웹 UI 포트 변경 (기본 8089)
locust -f locustfile.py --host=http://3.37.203.226:8000 --web-port=8090

# 헤드리스 (UI 없이, 터미널만)
locust -f locustfile.py --host=http://3.37.203.226:8000 --headless -u 20 -r 5 -t 60
# -u 20: 동시 사용자 20명
# -r 5: 초당 5명씩 증가
# -t 60: 60초 동안 실행
```

## 3. 웹 UI 사용

1. `locust` 실행 후 브라우저에서 **http://localhost:8089** 접속.
2. **Number of users**: 동시 가상 사용자 수 (예: 50).
3. **Ramp up**: 초당 몇 명씩 증가시킬지 (예: 5).
4. **Start swarming** 클릭.
5. **Statistics** 탭에서 RPS, 응답 시간(평균/백분위), 실패 수 확인.

## 4. 테스트 대상 API (locustfile.py)

| 비중 | 엔드포인트 | 설명 |
|------|------------|------|
| 10 | GET /api/health/ | 헬스 체크 |
| 8 | GET /api/dashboard/news/ | 뉴스 목록 |
| 8 | GET /api/dashboard/social/ | 소셜 목록 |
| 5 | GET /api/analyzer/analysis-results/ | 분석 결과 목록 |
| 3 | GET /api/analyzer/analysis/keywords/ | 키워드 분석 |
| 2 | GET /api/user_qa/history/ | QA 히스토리 |
| 1 | POST /api/user_qa/query/ | QA 질의 (부하 큼) |

비중은 상대 비율. POST /api/user_qa/query/ 는 CSRF 때문에 403이 나면, 테스트용으로 해당 출처를 `CSRF_TRUSTED_ORIGINS`에 넣거나 일시적으로 제외할 수 있음.

## 5. 부하 테스트 시 확인할 것

- **Locust UI**: RPS, 응답 시간, 실패율.
- **AWS CloudWatch**: EC2 CPU, 메모리, 네트워크.
- **Sentry**: 에러/예외 발생 여부.
- **서버 로그**: `docker compose logs -f web`, `tail -f logs/access.log`, `logs/error.log`.

---

## 5-1. Locust 지표 해석 & 성능 기준

### Response time이 계속 올라가면?

**맞아요. 보통 성능 문제 신호입니다.**

- 부하(동시 사용자)를 늘렸을 때 **응답 시간이 계속 증가**하면:
  - 서버가 **한계에 가까워지고 있다**는 뜻일 때가 많음 (CPU·메모리·DB·Rate limit 등).
- 반대로 **부하가 늘어도 응답 시간이 비슷하게 유지**되면:
  - 그 구간에서는 여유가 있다고 보면 됨.

---

### Locust에서 나오는 지표가 뭔지

| 지표 | 의미 | 좋을수록 높은지 / 낮은지 | 기준 (목표) |
|------|------|--------------------------|-------------|
| **RPS** (Requests Per Second) | 초당 처리한 요청 수. 처리량(throughput). | **높을수록 좋음** (같은 부하에서 더 많이 처리). | 부하(동시 사용자) 대비 유지·증가. 급감하지 않음. |
| **Failures** / **Failure %** | 실패한 요청 수·비율 (4xx/5xx, 타임아웃 등). | **낮을수록 좋음**. 0%가 목표. | **0%**. 1% 미만이면 보통, 1% 이상이면 나쁨. |
| **Median (50%)** | 응답 시간 중앙값. 절반은 이보다 빠름, 절반은 느림. | **낮을수록 좋음**. | 일반 API &lt; 200ms, 무거운 API &lt; 3초. |
| **Average (Avg)** | 응답 시간 평균. | **낮을수록 좋음**. | 일반 API &lt; 200ms, 무거운 API &lt; 3초. |
| **95%** | 상위 5% 느린 요청의 경계. “대부분 사용자” 체감에 가깝게 보려면 이걸 봄. | **낮을수록 좋음**. | 일반 API &lt; 500ms, 무거운 API &lt; 5초. |
| **99%** | 상위 1% 느린 요청. 극단적으로 느린 케이스. | **낮을수록 좋음**. | 95%의 2배 이하 권장. |

- **Response time** = 서버가 요청을 받고 응답을 돌려주기까지 걸린 시간.  
  **낮을수록 좋고**, 부하를 올려도 **크게 안 올라가면** 성능이 괜찮은 편.

---

### “얼마면 좋은지” 기준 (우리 API 참고)

일반적인 웹/API 기준이고, 서비스 요구에 따라 조정하면 됨.

| 구분 | 좋음 | 보통 | 나쁨 |
|------|------|------|------|
| **일반 API** (목록·헬스 등) | 평균 & 95% &lt; 200ms | &lt; 500ms | &gt; 1초 |
| **무거운 API** (QA 질의 등) | 평균 &lt; 3초 | &lt; 5초 | &gt; 10초 또는 타임아웃 |
| **실패율** | 0% | 1% 미만 | 1% 이상 |
| **RPS** | 목표 처리량 달성 | 목표의 80% 이상 | 목표의 50% 미만 또는 급감 |

- **Response time이 계속 올라간다** = 위 표의 “나쁨” 쪽으로 가고 있거나, 곧 그렇게 될 수 있다는 뜻으로 보면 됨.

---

### 그래프/차트로 보는 법

- **Response times (실시간 그래프)**  
  - 시간이 지나도 **대략 수평** → 부하를 잘 견디는 구간.  
  - **위로 계속 올라감** → 병목·한계에 가까워지는 구간 (성능 문제 의심).
- **RPS**  
  - 부하를 올렸는데 RPS가 **더 이상 안 올라가거나 떨어지면** → 서버가 한계라서 더 처리 못 하는 상태.
- **Failures**  
  - 429(Too Many Requests) → Rate limit.  
  - 5xx → 서버/DB 에러.  
  - 둘 다 **0에 가까울수록 좋음**.

---

### 한 줄 요약

- **Response time** → 낮을수록 좋고, **계속 올라가면 성능 문제 가능성 큼.**
- **RPS** → 높을수록 좋음 (처리량).
- **Failures / Failure %** → 0%가 목표.
- **Median, 95%, 99%** → 다 낮을수록 좋음 (체감 성능·꼬리 지연 줄이기).

---

### 우리 프로젝트 결과 vs 기준 비교

**리포트 기준:** `Locust_2026-03-09-15h28` (2분, 동시 사용자 최대 1,000명, 총 396 요청)

| 지표 | 기준 (목표) | 직전 테스트 결과 (2026-03-09) | 판정 |
|------|-------------|------------------------------|------|
| **Failure %** | 0% | 12건 / 396건 ≈ **3.0%** (전부 429 Too Many Requests) | **나쁨** — Rate limit 완화·조정 필요 |
| **RPS** | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 3.3 RPS** (동시 1,000명 구간) | **나쁨** — 목표 대비 낮고, 부하 증가 시 RPS 저하 |
| **일반 API 평균/95%** | &lt; 200ms / &lt; 500ms | 전체 평균 **54.8초**, 95% **104초** (health·news·social 등 모두 50초대) | **나쁨** — 목표 대비 크게 초과 |
| **무거운 API (QA) 평균** | &lt; 5초, 99% &lt; 15초 | 평균 **83.4초**, 95% 99초, 99% 99초 | **나쁨** — 목표 대비 크게 초과 |
| **Response time 추이** | 부하 올려도 수평 유지 | 부하 증가에 따라 **지속 상승** (초반 0.2초 → 말기 55초대) | **나쁨** — 병목·한계 구간 |

**엔드포인트별 실패 (429):**
- `/api/dashboard/news/?page_size=20` 5건  
- `/api/analyzer/analysis-results/?page_size=10` 3건  
- `/api/analyzer/analysis/keywords/` 2건  
- `/api/dashboard/social/?page_size=20` 2건  

**요약:** 실패율 0%·일반 API 200ms/500ms·QA 5초 목표에 전부 미달. Rate limit(429) 제거·상향, 응답 시간 단축(캐시·DB·외부 API 최적화), 동시 사용자당 RPS 개선이 필요함.

---

### 두 번째 테스트 (2026-03-09 16h19)

**리포트:** `Locust_2026-03-09-16h19` (2분, 동시 사용자 **100명**, 총 3,010 요청)

| 지표 | 기준 (목표) | 두 번째 테스트 결과 | 판정 |
|------|-------------|---------------------|------|
| **Failure %** | 0% | 2,177건 / 3,010건 ≈ **72.3%** (전부 429) | **나쁨** — Rate limit으로 대부분 실패 |
| **RPS** | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 25.1 RPS** (100명 구간) | **보통** — 1차 대비 처리량 상승, 목표 10 RPS 초과 |
| **일반 API 평균/95%** | &lt; 200ms / &lt; 500ms | 전체 평균 **1.87초**, 95% **6.3초** (성공 요청 기준) | **나쁨** — 목표 200ms/500ms에는 미달 |
| **무거운 API (QA) 평균** | &lt; 5초, 99% &lt; 15초 | 평균 **5.3초**, 95% 14초, 99% 20초 | **보통~나쁨** — 평균은 비슷, 99% 초과 |
| **Response time 추이** | 부하 올려도 수평 유지 | 100명 유지 구간에서 **1~3초대** 유지, 1차보다 안정 | 1차 대비 **개선** |

**엔드포인트별 실패 (429):**
- `/api/dashboard/news/?page_size=20` 596건  
- `/api/analyzer/analysis-results/?page_size=10` 541건  
- `/api/dashboard/social/?page_size=20` 567건  
- `/api/analyzer/analysis/keywords/` 473건  

**1차 vs 2차 요약:** 2차는 동시 사용자 100명으로 줄여 RPS(25)와 성공 시 응답 시간(1.87초)은 1차(1,000명, 3.3 RPS, 54초)보다 크게 나아짐. 다만 429가 2,177건으로 많아 **실패율 72%**로 목표 0%에는 여전히 미달. Rate limit을 완화하지 않으면 100명 구간에서도 실패율이 높게 나옴.

---

### 세 번째 테스트 (2026-03-11 15h13, Rate limit 비활성화)

**리포트:** `Locust_2026-03-11-15h13` (2분, 동시 사용자 **100명**, 총 1,152 요청, **실패 0건**)

| 지표 | 기준 (목표) | 세 번째 테스트 결과 | 판정 |
|------|-------------|---------------------|------|
| **Failure %** | 0% | **0건 / 1,152건 = 0%** (429 없음) | **좋음** — 목표 달성 |
| **RPS** | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 9.6 RPS** (100명 구간) | **보통** — 목표 10 RPS에 근접, 2차(25 RPS)보다 낮음 |
| **일반 API 평균/95%** | &lt; 200ms / &lt; 500ms | 전체 평균 **7.6초**, 95% **11초** | **나쁨** — 목표 200ms/500ms 대비 크게 초과 |
| **무거운 API (QA) 평균** | &lt; 5초, 99% &lt; 15초 | 평균 **12.1초**, 95% 22초, 99% 22초 | **나쁨** — 목표 대비 2배 이상 |
| **Response time 추이** | 부하 올려도 수평 유지 | 100명 유지 구간에서 **0.5초대 → 7.6초대로 지속 상승** | **나쁨** — 병목·한계 구간 |

**엔드포인트별 실패:** 없음 (429 제거 효과).

**2차 vs 3차 요약:** 3차는 Rate limit을 끄고 동일 100명으로 테스트. **실패율 0%**로 개선되었으나, 429가 사라지면서 모든 요청이 서버를 통과해 **응답 시간이 2차보다 악화** (평균 1.87초 → 7.6초, 95% 6.3초 → 11초). RPS는 25 → 9.6으로 감소. 즉, 제한을 풀었을 때 **처리량·지연 모두 한계에 도달**한 상태로 보는 것이 타당함.

### 세 번째 테스트 이후 개선 제안

- **실패율 0%**는 달성했으므로, 이제 **응답 시간·RPS** 쪽을 줄이는 게 목표.

- **일반 API(목록·조회)가 7초대인 이유:** 100명이 동시에 요청할 때 워커 수·DB/Redis 대기·**직렬화(응답 크기)** 등에서 병목이 있을 가능성이 큼.  
  - **Gunicorn 워커 수**를 (CPU·메모리 허용 범위에서) 늘리기.  
  - **DB**: 인덱스·N+1 제거, 자주 쓰는 목록/집계는 **Redis 캐시**로 짧은 TTL이라도 두기.  
  - **직렬화**: 응답 크기가 큰 엔드포인트는 필드 축소·페이지 크기 제한 검토 (아래 analysis-results 5.5MB 대응 참고).

- **analysis-results API 응답 ~5.5MB 원인 (교차 검증됨):**  
  `/api/analyzer/analysis-results/` 목록이 5.5MB급으로 나오는 이유는 아래 세 가지가 겹치기 때문이다.  
  - ① **pagination_class = None** → 최대 1,000건을 한 번에 반환 (`analyzer/views.py` 56–59행 `TrendAnalysisResultViewSet`).  
  - ② **fields = "__all__"** → `result_data`, `summary`, `parameters` 등 모든 필드 포함 (`analyzer/serializers.py` 6–11행 `TrendAnalysisResultSerializer`).  
  - ③ **result_data (JSONField)** → 건당 수백 KB~2MB급 거대 JSON (`analyzer/models.py`).  
  **수정 방안:**  
  - **FIX 1 (최우선):** `analyzer/views.py`에 `PageNumberPagination` 추가 (예: page_size=20, max_page_size=100), `TrendAnalysisResultViewSet`의 `pagination_class = None` 제거, `queryset`에서 `[:1000]` 제거. → 1,000건 → 20건으로 감소.  
  - **FIX 2:** `analyzer/serializers.py`에 목록용 **경량 Serializer** 추가 (필드: `id`, `analysis_type`, `platform`, `days`, `status`, `created_at`만, `result_data`/`summary`/`parameters` 제외). ViewSet에서 `get_serializer_class()`로 `list`일 때 경량, `retrieve`일 때 기존 전체 필드 사용. → 건당 수백 KB → 수백 바이트 수준.  
  - **FIX 3:** `BaseAnalysisViewSet`(109–116행)에도 동일하게 페이지네이션 + 목록/상세 Serializer 분리 적용 (`queryset` `[:100]` 제거, pagination 추가).  
  적용 시 목록 응답은 약 5.5MB → FIX 1만 적용 시 ~110KB, FIX 1+2 적용 시 ~6KB 수준으로 감소. 상세 조회(`/api/analyzer/analysis-results/{id}/`)는 기존처럼 전체 데이터 반환하므로 기능 유지. **프론트엔드**는 목록 응답이 `{ results, count, next, previous }` 형태로 바뀌면 해당 구조에 맞게 수정 필요.

- **QA(POST /api/user_qa/query/) 12초·99% 22초:** LLM 호출이 무거우므로, 동기 처리 대신 **요청 접수 후 비동기 처리·폴링 또는 웹소켓**으로 분리하면 다른 API의 대기 시간에 덜 영향을 줌.  
  또는 QA 전용 **별도 워커/스케일**을 두고, 나머지 API는 빠르게 유지.

- **Response time이 구간 내에서 계속 상승하는 것:** 100명 유지 구간에서도 시간이 지날수록 지연이 커지면, **메모리 누수·연결 풀 고갈·큐 적체** 가능성 있음.  
  - 로그·메트릭으로 **메모리·DB/Redis 연결 수** 추이 확인.  
  - 필요 시 **타임아웃·max_requests** 등으로 워커 재시작 정책 적용.

- **우선순위 제안:**  
  (1) **analysis-results 페이지네이션 + 목록/상세 Serializer 분리** (위 FIX 1·2·3) → 응답 크기·직렬화 부하 대폭 감소.  
  (2) 워커 수·리소스 확인 및 소폭 증설.  
  (3) 목록 API에 짧은 TTL Redis 캐시 도입.  
  (4) QA는 비동기/별도 스케일 검토.
---

### 네 번째 테스트 (2026-03-11 15h48, Gunicorn 워커 3→4)

**리포트:** `Locust_2026-03-11-15h48` (2분, 동시 사용자 **100명**, 총 1,108 요청, **실패 0건**)

| 지표 | 기준 (목표) | 네 번째 테스트 결과 | 판정 |
|------|-------------|---------------------|------|
| **Failure %** | 0% | **0건 / 1,108건 = 0%** (429 없음) | **좋음** — 목표 달성 |
| **RPS** | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 9.3 RPS** (100명 구간) | **보통** — 3차(9.6)와 비슷, 목표 10 RPS 근접 |
| **일반 API 평균/95%** | &lt; 200ms / &lt; 500ms | 전체 평균 **8.1초**, 95% **27초** | **나쁨** — 목표 대비 크게 초과, 95%는 꼬리 지연 큼 |
| **무거운 API (QA) 평균** | &lt; 5초, 99% &lt; 15초 | 평균 **16.1초**, 95% 29초, 99% 31초 | **나쁨** — 3차(12.1초·22초)보다 악화 |
| **Response time 추이** | 부하 올려도 수평 유지 | 초반 **0.2~1초대** → 중간 **12초대 스파이크** → 후반 **8초대**로 완만히 유지 | **나쁨** — 스파이크 후 구간 내 상승 |

**엔드포인트별 실패:** 없음.

**3차 vs 4차 요약:** 워커만 3→4로 올린 뒤 동일 100명·Rate limit 비활성화로 재테스트. 실패율 0% 유지. RPS는 9.6 → 9.3으로 거의 동일. 평균 응답 시간은 7.6초 → 8.1초로 소폭 악화, QA는 12.1초 → 16.1초로 악화. 95%·99%는 3차(11초·22초)보다 4차(27초·31초)가 더 나쁨. **워커 1개 추가만으로는 체감 개선이 없고**, analysis-results 등 대용량 응답·직렬화 부하 감소(FIX 1·2)가 선행될 필요가 있음.

---

### 다섯 번째 테스트 (2026-03-11 16h24, 워커 4 + analysis-results 페이지네이션·경량 Serializer)

**리포트:** `Locust_2026-03-11-16h24` (2분, 동시 사용자 **100명**, 총 2,355 요청, **실패 0건**)

| 지표 | 기준 (목표) | 다섯 번째 테스트 결과 | 판정 |
|------|-------------|------------------------|------|
| **Failure %** | 0% | **0건 / 2,355건 = 0%** (429 없음) | **좋음** — 목표 달성 |
| **RPS** | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 19.6 RPS** (100명 구간) | **좋음** — 4차(9.3)의 2배 이상, 목표 10 RPS 초과 |
| **일반 API 평균/95%** | &lt; 200ms / &lt; 500ms | 전체 평균 **2.8초**, 95% **6초** | **나쁨** — 목표에는 미달이나 4차(8.1초·27초) 대비 **대폭 개선** |
| **무거운 API (QA) 평균** | &lt; 5초, 99% &lt; 15초 | 평균 **8.9초**, 95% 33초, 99% 34초 | **나쁨** — 목표에는 미달, 4차(16.1초)보다 개선 |
| **Response time 추이** | 부하 올려도 수평 유지 | 초반 **0.1~0.7초대** → 중간 **7초대 스파이크** → 후반 **2.8초대**로 안정 | **보통** — 4차 대비 구간 내 수평에 가깝게 유지 |

**엔드포인트별 실패:** 없음.

**4차 vs 5차 요약:** 워커 4 + analysis-results **페이지네이션 + 목록 경량 Serializer** 적용 후 동일 100명·Rate limit 비활성화로 재테스트. **RPS 9.3 → 19.6**으로 약 2배 증가. 전체 평균 응답 시간 **8.1초 → 2.8초**, 95% **27초 → 6초**로 크게 개선. analysis-results 목록 응답 크기가 **5.5MB급 → ~1.5KB** 수준으로 줄어 직렬화·네트워크 부하가 감소한 효과. 일반 API는 여전히 200ms/500ms 목표에는 미달하나, **FIX 1·2 적용 효과가 뚜렷함**. QA(8.9초·99% 34초)는 목표에는 미달, 추가 개선 여지 있음.

---

### 다섯 번째 테스트 이후 추가 개선 제안

5차까지 적용한 것: Rate limit 비활성화(테스트용), 워커 4, analysis-results 페이지네이션·경량 Serializer. 아래는 **아직 목표에 미달한 부분**과 그에 대한 수정 방향.

- **일반 API 평균 2.8초·95% 6초 (목표 200ms/500ms 미달)**  
  - **Redis 캐시**: news, social, analysis-results 목록·keywords 등 자주 쓰는 GET에 짧은 TTL(예: 30초~1분) 캐시 도입 시 DB·직렬화 부하 감소.  
  - **DB**: 목록 쿼리 인덱스·`select_related`/`prefetch_related` 확인, N+1 제거.  
  - **직렬화**: news/social 등 다른 목록 API도 응답 필드·페이지 크기 점검해 필요 시 경량화.

- **QA 평균 8.9초·99% 34초 (목표 5초/15초 미달)**  
  - **비동기·폴링**: 요청 접수 후 Celery 등으로 처리하고, 클라이언트는 job_id로 폴링 또는 웹소켓으로 결과 수신. 동기 블로킹 시간 단축.  
  - **QA 전용 리소스**: QA만 받는 별도 워커/스케일을 두어 다른 API와 분리하면 일반 API 지연에 덜 영향.

- **중간 구간 7초대 스파이크**  
  - 100명 유지 구간 중 일시적으로 7초대까지 오른 뒤 2.8초대로 회복된 것은, QA·일부 무거운 요청이 몰렸을 때의 꼬리 지연으로 보임.  
  - QA 비동기화·캐시로 일반 API 부하를 줄이면 스파이크 완화 기대.

- **우선순위 제안 (5차 이후)**  
  (1) **목록 API Redis 캐시** (news, social, keywords 등) → 2.8초를 200ms~500ms 근처로 끌어내기.  
  (2) **DB·쿼리 점검** (인덱스, N+1).  
  (3) **QA 비동기·폴링 또는 별도 스케일** 검토.  
  (4) 개선 후 동일 조건(100명, 2분)으로 재테스트해 목표치 대비 재기록.

---

### 여섯 번째 테스트 (2026-03-13 14h48, 동시 10,000명, **Redis 적용 전**)

**리포트:** `Locust_2026-03-13-14h48` (2분 2초, 동시 사용자 **10,000명**, QA POST 제외). **목록 API에 Redis 캐시 적용 이전** 상태.

| 지표 | 기준 (목표) | 여섯 번째 테스트 결과 | 판정 |
|------|-------------|-----------------------|------|
| **Failure %** | 0% | **35,828 실패 / 41,261 총 ≈ 86.9%** (ConnectionReset·ConnectTimeout 다수) | **나쁨** — 목표 미달 |
| **RPS** | 부하 대비 유지 | **약 336.7 RPS** (10,000명 구간) | 처리량은 나오나 실패 다수 |
| **일반 API 평균/95%** | < 200ms / < 500ms | 전체 평균 **약 21.8초(21,758ms)**, 95% **47초(47,000ms)** | **나쁨** — 극심한 지연 |
| **무거운 API (QA)** | 평균 < 5초, 99% < 15초 | QA POST는 이번 테스트에서 제외 | - |
| **Response time 추이** | 부하 올려도 수평 유지 | 10,000명 구간에서 평균 ~21초, 95% ~47초로 매우 높음 | **나쁨** |

**엔드포인트별 통계 (요약, Redis 적용 전):**

- `/api/analyzer/analysis-results/?page_size=10`  
  - Requests: 8,906, Failures: 7,683, Avg: 21,892ms, 95%: 47,000ms, RPS: 72.67
- `/api/analyzer/analysis/keywords/`  
  - Requests: 7,228, Failures: 6,265, Avg: 21,746ms, 95%: 47,000ms, RPS: 58.98
- `/api/dashboard/news/?page_size=20`  
  - Requests: 9,035, Failures: 7,838, Avg: 21,686ms, 95%: 47,000ms, RPS: 73.72
- `/api/dashboard/social/?page_size=20`  
  - Requests: 8,937, Failures: 7,795, Avg: 21,668ms, 95%: 47,000ms, RPS: 72.92
- `/api/user_qa/history/?page_size=10`  
  - Requests: 7,155, Failures: 6,247, Avg: 21,806ms, 95%: 47,000ms, RPS: 58.38
- **집계**  
  - 총 요청 41,261, 실패 35,828, 평균 21,758ms, 95% 47,000ms, 99% 54,000ms, RPS 336.67

**요약:**

- **Redis 적용 전** 동시 10,000명 테스트에서 실패율 **약 86.9%**, 평균 응답 **약 21.8초**, 95% **47초**로 목표와 크게 벗어남.  
- ConnectionResetError·ConnectTimeout 다수로 서버 한계 초과·타임아웃 발생. Redis 캐시 도입으로 7차에서 개선됨.

---

### 일곱 번째 테스트 (2026-03-16 19h49, 동시 1,000명, **Redis 적용 후**)

**리포트:** `Locust_2026-03-16-19h49` (3분 5초, 동시 사용자 **10,000명**, `user_qa_query`는 @task(0)으로 제외). **목록 API에 Redis 캐시 적용 이후** 상태.

| 지표 | 기준 (목표) | 일곱 번째 테스트 결과 | 판정 |
|------|-------------|-----------------------|------|
| **Failure %** | 0% | **0건 / 62,137건 = 0%** | **좋음** — 목표 달성 |
| **RPS** | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 335.6 RPS** (10,000명 구간) | **좋음** — 매우 높은 처리량 |
| **일반 API 평균/95%** | < 200ms / < 500ms | 전체 평균 **0.86초(857ms)**, 95% **3.2초(3200ms)** | **나쁨** — 목표에는 미달, 5차(2.8초/6초)보다는 개선 |
| **무거운 API (QA)** | 평균 < 5초, 99% < 15초 | QA POST는 이번 테스트에서 제외 | - |
| **Response time 추이** | 부하 올려도 수평 유지 | 10,000명 유지 구간에서 평균 ~0.8초, 95% ~3초대 수준 유지 | **보통** — 3·4·5차보다 안정적이지만 목표보다는 느림 |

**엔드포인트별 통계 (요약, Redis 적용 후):**

- `/api/analyzer/analysis-results/?page_size=10`  
  - Requests: 13,351, Failures: 0, Avg: 841.78ms, 95%: 3200ms, RPS: 72.11
- `/api/analyzer/analysis/keywords/`  
  - Requests: 10,922, Failures: 0, Avg: 841.79ms, 95%: 3100ms, RPS: 58.99
- `/api/dashboard/news/?page_size=20`  
  - Requests: 13,463, Failures: 0, Avg: 849.65ms, 95%: 3100ms, RPS: 72.71
- `/api/dashboard/social/?page_size=20`  
  - Requests: 13,455, Failures: 0, Avg: 854.56ms, 95%: 3200ms, RPS: 72.67
- `/api/user_qa/history/?page_size=10`  
  - Requests: 10,946, Failures: 0, Avg: 905.8ms, 95%: 3300ms, RPS: 59.12

**요약:**

- **Redis 적용 후** 동시 10,000명에서 **실패율 0%**, 총 RPS **약 335.6**으로 매우 높은 처리량을 유지했다.  
- Redis 캐시·analysis-results 최적화로 6차(Redis 적용 전) 대비 **평균 21.8초 → 0.86초, 95% 47초 → 3.2초**로 응답 시간이 크게 개선되었다.  
- 다만 프로젝트 목표(일반 API 평균 < 200ms, 95% < 500ms) 관점에서는 여전히 **목표 미달**이며, 99% 지연이 ~7초 수준으로 꼬리 지연도 남아 있다.

---

### 목표치 (이번 프로젝트)

| 구분 | 목표치 | 비고 |
|------|--------|------|
| **실패율** | **0%** | 429·5xx 없음. Rate limit·타임아웃 설정 조정으로 달성 목표. |
| **일반 API** (health, news, social, analysis-results, keywords, history) | 평균 &lt; 200ms, 95% &lt; 500ms | 목록·조회 위주. |
| **무거운 API** (POST /api/user_qa/query/) | 평균 &lt; 5초, 99% &lt; 15초 | LLM 호출 포함. |
| **RPS** | 동시 사용자 50명 기준 초당 10 RPS 이상 유지 | 부하 시 RPS 급감 없음. |
| **Response time 그래프** | 부하 구간에서 수평 유지 | 계속 상승하지 않음. |

- 테스트할 때마다 위 목표치와 실제 값을 비교해 기록해 두면 개선 여부를 확인하기 쉽다.

---

## 6. 주의

- Locust는 **서버(EC2)가 아닌 로컬 PC**에서 실행하는 것을 권장 (같은 서버에서 돌리면 부하 생성이 서버 지표를 왜곡함).
- EC2 보안 그룹에서 **8000 포트**가 Locust를 실행하는 PC IP(또는 0.0.0.0)에 열려 있어야 함.
