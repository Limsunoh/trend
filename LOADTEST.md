# Locust 부하 테스트

대용량 트래픽을 시뮬레이션하고 CloudWatch·Sentry·로그와 함께 관찰하기 위한 가이드.

## 1. 설치 (로컬 PC, 서버와 다른 머신 권장)

```bash
pip install locust
```

프로젝트 가상환경에 넣거나, 별도 venv 사용 가능.

## 2. 실행

프로젝트 루트에서:

```bash
# EC2 서버 대상 (기본)
locust -f locustfile.py --host=http://3.37.203.226:8000

# 웹 UI 포트 변경 (기본 8089)
locust -f locustfile.py --host=http://3.37.203.226:8000 --web-port=8090

# 헤드리스 (UI 없이, 터미널만)
locust -f locustfile.py --host=http://3.37.203.226:8000 --headless -u 20 -r 5 -t 60
# -u 20: 동시 사용자 20명
# -r 5: 초당 5명씩 증가
# -t 60: 60초 동안 실행
```

## 3. 웹 UI 사용

1. `locust` 실행 후 브라우저에서 **[http://localhost:8089](http://localhost:8089)** 접속.
2. **Number of users**: 동시 가상 사용자 수 (예: 50).
3. **Ramp up**: 초당 몇 명씩 증가시킬지 (예: 5).
4. **Start swarming** 클릭.
5. **Statistics** 탭에서 RPS, 응답 시간(평균/백분위), 실패 수 확인.

## 4. 테스트 대상 API (locustfile.py)


| 비중  | 엔드포인트                                | 설명           |
| --- | ------------------------------------ | ------------ |
| 10  | GET /api/health/                     | 헬스 체크        |
| 8   | GET /api/dashboard/news/             | 뉴스 목록        |
| 8   | GET /api/dashboard/social/           | 소셜 목록        |
| 5   | GET /api/analyzer/analysis-results/  | 분석 결과 목록     |
| 3   | GET /api/analyzer/analysis/keywords/ | 키워드 분석       |
| 2   | GET /api/user_qa/history/            | QA 히스토리      |
| 1   | POST /api/user_qa/query/             | QA 질의 (부하 큼) |


비중은 상대 비율. POST /api/user_qa/query/ 는 CSRF 때문에 403이 나면, 테스트용으로 해당 출처를 `CSRF_TRUSTED_ORIGINS`에 넣거나 일시적으로 제외할 수 있음.

### 목록 API Redis 캐시 TTL (서버 `.env`)

`load_dotenv()`로 읽는 루트 `.env`에 둡니다 (`trend_analyzer/settings.py`).


| 변수                              | 적용 대상                                                  | 기본값(초) |
| ------------------------------- | ------------------------------------------------------ | ------ |
| `LIST_API_CACHE_TTL_DASHBOARD`  | 뉴스·소셜 목록 (`/api/dashboard/news/`, `.../social/`)       | 120    |
| `LIST_API_CACHE_TTL_ANALYZER`   | 분석 결과·keywords 등 (`analysis-results`, `analysis/*` 목록) | 600    |
| `LIST_API_CACHE_TTL_QA_HISTORY` | QA 히스토리 (`/api/user_qa/history/`)                      | 60     |


**하위 호환:** `LIST_API_CACHE_TTL`만 설정하면 위 세 그룹을 모두 같은 값으로 덮어씁니다. 원인 분리·튜닝 시에는 그룹 변수를 각각 두는 편이 낫습니다.

## 5. 부하 테스트 시 확인할 것

- **Locust UI**: RPS, 응답 시간, 실패율.
- **AWS CloudWatch**: EC2 CPU, 메모리, 네트워크.
- **Sentry**: 에러/예외 발생 여부.
- **서버 로그**: `docker compose logs -f web`, `tail -f logs/access.log`, `logs/error.log`.

---

## 5-1. Locust 지표 해석 & 성능 기준

### Response time이 계속 올라가면?

**맞아요. 보통 성능 문제 신호입니다.**

- 부하(동시 사용자)를 늘렸을 때 **응답 시간이 계속 증가**하면:
  - 서버가 **한계에 가까워지고 있다**는 뜻일 때가 많음 (CPU·메모리·DB·Rate limit 등).
- 반대로 **부하가 늘어도 응답 시간이 비슷하게 유지**되면:
  - 그 구간에서는 여유가 있다고 보면 됨.

---

### Locust에서 나오는 지표가 뭔지


| 지표                            | 의미                                           | 좋을수록 높은지 / 낮은지                 | 기준 (목표)                         |
| ----------------------------- | -------------------------------------------- | ------------------------------ | ------------------------------- |
| **RPS** (Requests Per Second) | 초당 처리한 요청 수. 처리량(throughput).                | **높을수록 좋음** (같은 부하에서 더 많이 처리). | 부하(동시 사용자) 대비 유지·증가. 급감하지 않음.   |
| **Failures** / **Failure %**  | 실패한 요청 수·비율 (4xx/5xx, 타임아웃 등).               | **낮을수록 좋음**. 0%가 목표.           | **0%**. 1% 미만이면 보통, 1% 이상이면 나쁨. |
| **Median (50%)**              | 응답 시간 중앙값. 절반은 이보다 빠름, 절반은 느림.               | **낮을수록 좋음**.                   | 일반 API < 200ms, 무거운 API < 3초.   |
| **Average (Avg)**             | 응답 시간 평균.                                    | **낮을수록 좋음**.                   | 일반 API < 200ms, 무거운 API < 3초.   |
| **95%**                       | 상위 5% 느린 요청의 경계. “대부분 사용자” 체감에 가깝게 보려면 이걸 봄. | **낮을수록 좋음**.                   | 일반 API < 500ms, 무거운 API < 5초.   |
| **99%**                       | 상위 1% 느린 요청. 극단적으로 느린 케이스.                   | **낮을수록 좋음**.                   | 95%의 2배 이하 권장.                  |


- **Response time** = 서버가 요청을 받고 응답을 돌려주기까지 걸린 시간.  
**낮을수록 좋고**, 부하를 올려도 **크게 안 올라가면** 성능이 괜찮은 편.

---

### “얼마면 좋은지” 기준 (우리 API 참고)

일반적인 웹/API 기준이고, 서비스 요구에 따라 조정하면 됨.


| 구분                    | 좋음               | 보통         | 나쁨               |
| --------------------- | ---------------- | ---------- | ---------------- |
| **일반 API** (목록·헬스 등)  | 평균 & 95% < 200ms | < 500ms    | > 1초             |
| **무거운 API** (QA 질의 등) | 평균 < 3초          | < 5초       | > 10초 또는 타임아웃    |
| **실패율**               | 0%               | 1% 미만      | 1% 이상            |
| **RPS**               | 목표 처리량 달성        | 목표의 80% 이상 | 목표의 50% 미만 또는 급감 |


- **Response time이 계속 올라간다** = 위 표의 “나쁨” 쪽으로 가고 있거나, 곧 그렇게 될 수 있다는 뜻으로 보면 됨.

---

### 그래프/차트로 보는 법

- **Response times (실시간 그래프)**  
  - 시간이 지나도 **대략 수평** → 부하를 잘 견디는 구간.  
  - **위로 계속 올라감** → 병목·한계에 가까워지는 구간 (성능 문제 의심).
- **RPS**  
  - 부하를 올렸는데 RPS가 **더 이상 안 올라가거나 떨어지면** → 서버가 한계라서 더 처리 못 하는 상태.
- **Failures**  
  - 429(Too Many Requests) → Rate limit.  
  - 5xx → 서버/DB 에러.  
  - 둘 다 **0에 가까울수록 좋음**.

---

### 한 줄 요약

- **Response time** → 낮을수록 좋고, **계속 올라가면 성능 문제 가능성 큼.**
- **RPS** → 높을수록 좋음 (처리량).
- **Failures / Failure %** → 0%가 목표.
- **Median, 95%, 99%** → 다 낮을수록 좋음 (체감 성능·꼬리 지연 줄이기).

---

### CloudWatch 한 줄 판정 규칙

- **EC2 CPU**: 평균 70% 이상 또는 최대 90% 이상이면 CPU 병목 위험으로 판정.
- **EC2 메모리**: `mem_used_percent` 75% 이상이면 메모리 압박 구간으로 판정.
- **RDS 부하**: CPU 20% 이상이 지속되거나 IOPS 스파이크가 반복되면 DB 병목 후보로 판정.
- **안정성**: 5xx 비율 0.1% 이상 또는 timeout/reset 로그 증가 시 안정성 이슈로 판정.
- **정합성**: Locust 대비 CloudWatch 값이 과소/과대면 시간창(UTC/KST) 또는 경로(Caddy 경유/직결)부터 재검증.

---

### 우리 프로젝트 결과 vs 기준 비교

**리포트 기준:** `Locust_2026-03-09-15h28` (2분, 동시 사용자 최대 1,000명, 총 396 요청)


| 지표                   | 기준 (목표)                    | 직전 테스트 결과 (2026-03-09)                                       | 판정                                |
| -------------------- | -------------------------- | ------------------------------------------------------------ | --------------------------------- |
| **Failure %**        | 0%                         | 12건 / 396건 ≈ **3.0%** (전부 429 Too Many Requests)             | **나쁨** — Rate limit 완화·조정 필요      |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 3.3 RPS** (동시 1,000명 구간)                                 | **나쁨** — 목표 대비 낮고, 부하 증가 시 RPS 저하 |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **54.8초**, 95% **104초** (health·news·social 등 모두 50초대) | **나쁨** — 목표 대비 크게 초과              |
| **무거운 API (QA) 평균**  | < 5초, 99% < 15초            | 평균 **83.4초**, 95% 99초, 99% 99초                               | **나쁨** — 목표 대비 크게 초과              |
| **Response time 추이** | 부하 올려도 수평 유지               | 부하 증가에 따라 **지속 상승** (초반 0.2초 → 말기 55초대)                      | **나쁨** — 병목·한계 구간                 |


**엔드포인트별 실패 (429):**

- `/api/dashboard/news/?page_size=20` 5건  
- `/api/analyzer/analysis-results/?page_size=10` 3건  
- `/api/analyzer/analysis/keywords/` 2건  
- `/api/dashboard/social/?page_size=20` 2건

**요약:** 실패율 0%·일반 API 200ms/500ms·QA 5초 목표에 전부 미달. Rate limit(429) 제거·상향, 응답 시간 단축(캐시·DB·외부 API 최적화), 동시 사용자당 RPS 개선이 필요함.

**CloudWatch MCP 실측(해당 시간창):**

- `trend/web-access` 로그 그룹 생성 이전 구간이라 조회 불가.
- 로그 그룹 생성 시각: 2026-03-13 14:35:06(KST) 이후 데이터만 조회 가능.
- 결론: 1차는 CloudWatch 로그 실측 비교가 불가능.
- EC2 CPU/메모리, RDS CPU/IOPS도 동일 시간창에서 Datapoint가 없어 `N/A`.
- 한 줄 판정: **CloudWatch 실측 부재 구간**이라 시스템 병목 판단 불가.

---

### 두 번째 테스트 (2026-03-09 16h19)

**리포트:** `Locust_2026-03-09-16h19` (2분, 동시 사용자 **100명**, 총 3,010 요청)


| 지표                   | 기준 (목표)                    | 두 번째 테스트 결과                              | 판정                                  |
| -------------------- | -------------------------- | ---------------------------------------- | ----------------------------------- |
| **Failure %**        | 0%                         | 2,177건 / 3,010건 ≈ **72.3%** (전부 429)     | **나쁨** — Rate limit으로 대부분 실패        |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 25.1 RPS** (100명 구간)                 | **보통** — 1차 대비 처리량 상승, 목표 10 RPS 초과 |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **1.87초**, 95% **6.3초** (성공 요청 기준) | **나쁨** — 목표 200ms/500ms에는 미달        |
| **무거운 API (QA) 평균**  | < 5초, 99% < 15초            | 평균 **5.3초**, 95% 14초, 99% 20초            | **보통~나쁨** — 평균은 비슷, 99% 초과          |
| **Response time 추이** | 부하 올려도 수평 유지               | 100명 유지 구간에서 **1~3초대** 유지, 1차보다 안정       | 1차 대비 **개선**                        |


**엔드포인트별 실패 (429):**

- `/api/dashboard/news/?page_size=20` 596건  
- `/api/analyzer/analysis-results/?page_size=10` 541건  
- `/api/dashboard/social/?page_size=20` 567건  
- `/api/analyzer/analysis/keywords/` 473건

**1차 vs 2차 요약:** 2차는 동시 사용자 100명으로 줄여 RPS(25)와 성공 시 응답 시간(1.87초)은 1차(1,000명, 3.3 RPS, 54초)보다 크게 나아짐. 다만 429가 2,177건으로 많아 **실패율 72%**로 목표 0%에는 여전히 미달. Rate limit을 완화하지 않으면 100명 구간에서도 실패율이 높게 나옴.

**CloudWatch MCP 실측(해당 시간창):**

- `trend/web-access` 로그 그룹 생성 이전 구간이라 조회 불가.
- 결론: 2차는 CloudWatch 로그 실측 비교가 불가능.
- EC2 CPU/메모리, RDS CPU/IOPS도 동일 시간창에서 Datapoint가 없어 `N/A`.
- 한 줄 판정: **CloudWatch 실측 부재 구간**이라 시스템 병목 판단 불가.

---

### 세 번째 테스트 (2026-03-11 15h13, Rate limit 비활성화)

**리포트:** `Locust_2026-03-11-15h13` (2분, 동시 사용자 **100명**, 총 1,152 요청, **실패 0건**)


| 지표                   | 기준 (목표)                    | 세 번째 테스트 결과                           | 판정                                      |
| -------------------- | -------------------------- | ------------------------------------- | --------------------------------------- |
| **Failure %**        | 0%                         | **0건 / 1,152건 = 0%** (429 없음)         | **좋음** — 목표 달성                          |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 9.6 RPS** (100명 구간)               | **보통** — 목표 10 RPS에 근접, 2차(25 RPS)보다 낮음 |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **7.6초**, 95% **11초**           | **나쁨** — 목표 200ms/500ms 대비 크게 초과        |
| **무거운 API (QA) 평균**  | < 5초, 99% < 15초            | 평균 **12.1초**, 95% 22초, 99% 22초        | **나쁨** — 목표 대비 2배 이상                    |
| **Response time 추이** | 부하 올려도 수평 유지               | 100명 유지 구간에서 **0.5초대 → 7.6초대로 지속 상승** | **나쁨** — 병목·한계 구간                       |


**엔드포인트별 실패:** 없음 (429 제거 효과).

**2차 vs 3차 요약:** 3차는 Rate limit을 끄고 동일 100명으로 테스트. **실패율 0%**로 개선되었으나, 429가 사라지면서 모든 요청이 서버를 통과해 **응답 시간이 2차보다 악화** (평균 1.87초 → 7.6초, 95% 6.3초 → 11초). RPS는 25 → 9.6으로 감소. 즉, 제한을 풀었을 때 **처리량·지연 모두 한계에 도달**한 상태로 보는 것이 타당함.

**CloudWatch MCP 실측(해당 시간창):**

- `trend/web-access` 로그 그룹 생성 이전 구간이라 조회 불가.
- 결론: 3차는 CloudWatch 로그 실측 비교가 불가능.
- EC2 CPU/메모리, RDS CPU/IOPS도 동일 시간창에서 Datapoint가 없어 `N/A`.
- 한 줄 판정: **CloudWatch 실측 부재 구간**이라 시스템 병목 판단 불가.

### 세 번째 테스트 이후 개선 제안

- **실패율 0%**는 달성했으므로, 이제 **응답 시간·RPS** 쪽을 줄이는 게 목표.
- **일반 API(목록·조회)가 7초대인 이유:** 100명이 동시에 요청할 때 워커 수·DB/Redis 대기·**직렬화(응답 크기)** 등에서 병목이 있을 가능성이 큼.  
  - **Gunicorn 워커 수**를 (CPU·메모리 허용 범위에서) 늘리기.  
  - **DB**: 인덱스·N+1 제거, 자주 쓰는 목록/집계는 **Redis 캐시**로 짧은 TTL이라도 두기.  
  - **직렬화**: 응답 크기가 큰 엔드포인트는 필드 축소·페이지 크기 제한 검토 (아래 analysis-results 5.5MB 대응 참고).
- **analysis-results API 응답 ~5.5MB 원인 (교차 검증됨):**  
`/api/analyzer/analysis-results/` 목록이 5.5MB급으로 나오는 이유는 아래 세 가지가 겹치기 때문이다.  
  - ① **pagination_class = None** → 최대 1,000건을 한 번에 반환 (`analyzer/views.py` 56–59행 `TrendAnalysisResultViewSet`).  
  - ② **fields = "all"** → `result_data`, `summary`, `parameters` 등 모든 필드 포함 (`analyzer/serializers.py` 6–11행 `TrendAnalysisResultSerializer`).  
  - ③ **result_data (JSONField)** → 건당 수백 KB~2MB급 거대 JSON (`analyzer/models.py`).  
  **수정 방안:**  
  - **FIX 1 (최우선):** `analyzer/views.py`에 `PageNumberPagination` 추가 (예: page_size=20, max_page_size=100), `TrendAnalysisResultViewSet`의 `pagination_class = None` 제거, `queryset`에서 `[:1000]` 제거. → 1,000건 → 20건으로 감소.  
  - **FIX 2:** `analyzer/serializers.py`에 목록용 **경량 Serializer** 추가 (필드: `id`, `analysis_type`, `platform`, `days`, `status`, `created_at`만, `result_data`/`summary`/`parameters` 제외). ViewSet에서 `get_serializer_class()`로 `list`일 때 경량, `retrieve`일 때 기존 전체 필드 사용. → 건당 수백 KB → 수백 바이트 수준.  
  - **FIX 3:** `BaseAnalysisViewSet`(109–116행)에도 동일하게 페이지네이션 + 목록/상세 Serializer 분리 적용 (`queryset` `[:100]` 제거, pagination 추가).  
  적용 시 목록 응답은 약 5.5MB → FIX 1만 적용 시 ~110KB, FIX 1+2 적용 시 ~6KB 수준으로 감소. 상세 조회(`/api/analyzer/analysis-results/{id}/`)는 기존처럼 전체 데이터 반환하므로 기능 유지. **프론트엔드**는 목록 응답이 `{ results, count, next, previous }` 형태로 바뀌면 해당 구조에 맞게 수정 필요.
- **QA(POST /api/user_qa/query/) 12초·99% 22초:** LLM 호출이 무거우므로, 동기 처리 대신 **요청 접수 후 비동기 처리·폴링 또는 웹소켓**으로 분리하면 다른 API의 대기 시간에 덜 영향을 줌.  
또는 QA 전용 **별도 워커/스케일**을 두고, 나머지 API는 빠르게 유지.
- **Response time이 구간 내에서 계속 상승하는 것:** 100명 유지 구간에서도 시간이 지날수록 지연이 커지면, **메모리 누수·연결 풀 고갈·큐 적체** 가능성 있음.  
  - 로그·메트릭으로 **메모리·DB/Redis 연결 수** 추이 확인.  
  - 필요 시 **타임아웃·max_requests** 등으로 워커 재시작 정책 적용.
- **우선순위 제안:**  
(1) **analysis-results 페이지네이션 + 목록/상세 Serializer 분리** (위 FIX 1·2·3) → 응답 크기·직렬화 부하 대폭 감소.  
(2) 워커 수·리소스 확인 및 소폭 증설.  
(3) 목록 API에 짧은 TTL Redis 캐시 도입.  
(4) QA는 비동기/별도 스케일 검토.

---

### 네 번째 테스트 (2026-03-11 15h48, Gunicorn 워커 3→4)

**리포트:** `Locust_2026-03-11-15h48` (2분, 동시 사용자 **100명**, 총 1,108 요청, **실패 0건**)


| 지표                   | 기준 (목표)                    | 네 번째 테스트 결과                                            | 판정                                 |
| -------------------- | -------------------------- | ------------------------------------------------------ | ---------------------------------- |
| **Failure %**        | 0%                         | **0건 / 1,108건 = 0%** (429 없음)                          | **좋음** — 목표 달성                     |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 9.3 RPS** (100명 구간)                                | **보통** — 3차(9.6)와 비슷, 목표 10 RPS 근접 |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **8.1초**, 95% **27초**                            | **나쁨** — 목표 대비 크게 초과, 95%는 꼬리 지연 큼 |
| **무거운 API (QA) 평균**  | < 5초, 99% < 15초            | 평균 **16.1초**, 95% 29초, 99% 31초                         | **나쁨** — 3차(12.1초·22초)보다 악화        |
| **Response time 추이** | 부하 올려도 수평 유지               | 초반 **0.2~1초대** → 중간 **12초대 스파이크** → 후반 **8초대**로 완만히 유지 | **나쁨** — 스파이크 후 구간 내 상승            |


**엔드포인트별 실패:** 없음.

**3차 vs 4차 요약:** 워커만 3→4로 올린 뒤 동일 100명·Rate limit 비활성화로 재테스트. 실패율 0% 유지. RPS는 9.6 → 9.3으로 거의 동일. 평균 응답 시간은 7.6초 → 8.1초로 소폭 악화, QA는 12.1초 → 16.1초로 악화. 95%·99%는 3차(11초·22초)보다 4차(27초·31초)가 더 나쁨. **워커 1개 추가만으로는 체감 개선이 없고**, analysis-results 등 대용량 응답·직렬화 부하 감소(FIX 1·2)가 선행될 필요가 있음.

**CloudWatch MCP 실측(해당 시간창):**

- `trend/web-access` 로그 그룹 생성 이전 구간이라 조회 불가.
- 결론: 4차는 CloudWatch 로그 실측 비교가 불가능.
- EC2 CPU/메모리, RDS CPU/IOPS도 동일 시간창에서 Datapoint가 없어 `N/A`.
- 한 줄 판정: **CloudWatch 실측 부재 구간**이라 시스템 병목 판단 불가.

---

### 다섯 번째 테스트 (2026-03-11 16h24, 워커 4 + analysis-results 페이지네이션·경량 Serializer)

**리포트:** `Locust_2026-03-11-16h24` (2분, 동시 사용자 **100명**, 총 2,355 요청, **실패 0건**)


| 지표                   | 기준 (목표)                    | 다섯 번째 테스트 결과                                          | 판정                                           |
| -------------------- | -------------------------- | ----------------------------------------------------- | -------------------------------------------- |
| **Failure %**        | 0%                         | **0건 / 2,355건 = 0%** (429 없음)                         | **좋음** — 목표 달성                               |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 19.6 RPS** (100명 구간)                              | **좋음** — 4차(9.3)의 2배 이상, 목표 10 RPS 초과        |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **2.8초**, 95% **6초**                            | **나쁨** — 목표에는 미달이나 4차(8.1초·27초) 대비 **대폭 개선** |
| **무거운 API (QA) 평균**  | < 5초, 99% < 15초            | 평균 **8.9초**, 95% 33초, 99% 34초                         | **나쁨** — 목표에는 미달, 4차(16.1초)보다 개선             |
| **Response time 추이** | 부하 올려도 수평 유지               | 초반 **0.1~0.7초대** → 중간 **7초대 스파이크** → 후반 **2.8초대**로 안정 | **보통** — 4차 대비 구간 내 수평에 가깝게 유지               |


**엔드포인트별 실패:** 없음.

**4차 vs 5차 요약:** 워커 4 + analysis-results **페이지네이션 + 목록 경량 Serializer** 적용 후 동일 100명·Rate limit 비활성화로 재테스트. **RPS 9.3 → 19.6**으로 약 2배 증가. 전체 평균 응답 시간 **8.1초 → 2.8초**, 95% **27초 → 6초**로 크게 개선. analysis-results 목록 응답 크기가 **5.5MB급 → ~1.5KB** 수준으로 줄어 직렬화·네트워크 부하가 감소한 효과. 일반 API는 여전히 200ms/500ms 목표에는 미달하나, **FIX 1·2 적용 효과가 뚜렷함**. QA(8.9초·99% 34초)는 목표에는 미달, 추가 개선 여지 있음.

**CloudWatch MCP 실측(해당 시간창):**

- `trend/web-access` 로그 그룹 생성 이전 구간이라 조회 불가.
- 결론: 5차는 CloudWatch 로그 실측 비교가 불가능.
- EC2 CPU/메모리, RDS CPU/IOPS도 동일 시간창에서 Datapoint가 없어 `N/A`.
- 한 줄 판정: **CloudWatch 실측 부재 구간**이라 시스템 병목 판단 불가.

---

### 다섯 번째 테스트 이후 추가 개선 제안

5차까지 적용한 것: Rate limit 비활성화(테스트용), 워커 4, analysis-results 페이지네이션·경량 Serializer. 아래는 **아직 목표에 미달한 부분**과 그에 대한 수정 방향.

- **일반 API 평균 2.8초·95% 6초 (목표 200ms/500ms 미달)**  
  - **Redis 캐시**: news, social, analysis-results 목록·keywords 등 자주 쓰는 GET에 짧은 TTL(예: 30초~1분) 캐시 도입 시 DB·직렬화 부하 감소.  
  - **DB**: 목록 쿼리 인덱스·`select_related`/`prefetch_related` 확인, N+1 제거.  
  - **직렬화**: news/social 등 다른 목록 API도 응답 필드·페이지 크기 점검해 필요 시 경량화.
- **QA 평균 8.9초·99% 34초 (목표 5초/15초 미달)**  
  - **비동기·폴링**: 요청 접수 후 Celery 등으로 처리하고, 클라이언트는 job_id로 폴링 또는 웹소켓으로 결과 수신. 동기 블로킹 시간 단축.  
  - **QA 전용 리소스**: QA만 받는 별도 워커/스케일을 두어 다른 API와 분리하면 일반 API 지연에 덜 영향.
- **중간 구간 7초대 스파이크**  
  - 100명 유지 구간 중 일시적으로 7초대까지 오른 뒤 2.8초대로 회복된 것은, QA·일부 무거운 요청이 몰렸을 때의 꼬리 지연으로 보임.  
  - QA 비동기화·캐시로 일반 API 부하를 줄이면 스파이크 완화 기대.
- **우선순위 제안 (5차 이후)**  
(1) **목록 API Redis 캐시** (news, social, keywords 등) → 2.8초를 200ms~500ms 근처로 끌어내기.  
(2) **DB·쿼리 점검** (인덱스, N+1).  
(3) **QA 비동기·폴링 또는 별도 스케일** 검토.  
(4) 개선 후 동일 조건(100명, 2분)으로 재테스트해 목표치 대비 재기록.

---

### 여섯 번째 테스트 (2026-03-13 14h48, 동시 10,000명, **Redis 적용 전**)

**리포트:** `Locust_2026-03-13-14h48` (2분 2초, 동시 사용자 **10,000명**, QA POST 제외). **목록 API에 Redis 캐시 적용 이전** 상태.


| 지표                   | 기준 (목표)            | 여섯 번째 테스트 결과                                                         | 판정              |
| -------------------- | ------------------ | -------------------------------------------------------------------- | --------------- |
| **Failure %**        | 0%                 | **35,828 실패 / 41,261 총 ≈ 86.9%** (ConnectionReset·ConnectTimeout 다수) | **나쁨** — 목표 미달  |
| **RPS**              | 부하 대비 유지           | **약 336.7 RPS** (10,000명 구간)                                         | 처리량은 나오나 실패 다수  |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **약 21.8초(21,758ms)**, 95% **47초(47,000ms)**                   | **나쁨** — 극심한 지연 |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST는 이번 테스트에서 제외                                                 | -               |
| **Response time 추이** | 부하 올려도 수평 유지       | 10,000명 구간에서 평균 ~21초, 95% ~47초로 매우 높음                                | **나쁨**          |


**엔드포인트별 통계 (요약, Redis 적용 전):**

- `/api/analyzer/analysis-results/?page_size=10`  
  - Requests: 8,906, Failures: 7,683, Avg: 21,892ms, 95%: 47,000ms, RPS: 72.67
- `/api/analyzer/analysis/keywords/`  
  - Requests: 7,228, Failures: 6,265, Avg: 21,746ms, 95%: 47,000ms, RPS: 58.98
- `/api/dashboard/news/?page_size=20`  
  - Requests: 9,035, Failures: 7,838, Avg: 21,686ms, 95%: 47,000ms, RPS: 73.72
- `/api/dashboard/social/?page_size=20`  
  - Requests: 8,937, Failures: 7,795, Avg: 21,668ms, 95%: 47,000ms, RPS: 72.92
- `/api/user_qa/history/?page_size=10`  
  - Requests: 7,155, Failures: 6,247, Avg: 21,806ms, 95%: 47,000ms, RPS: 58.38
- **집계**  
  - 총 요청 41,261, 실패 35,828, 평균 21,758ms, 95% 47,000ms, 99% 54,000ms, RPS 336.67

**요약:**

- **Redis 적용 전** 동시 10,000명 테스트에서 실패율 **약 86.9%**, 평균 응답 **약 21.8초**, 95% **47초**로 목표와 크게 벗어남.  
- ConnectionResetError·ConnectTimeout 다수로 서버 한계 초과·타임아웃 발생. Redis 캐시 도입으로 7차에서 개선됨.

**CloudWatch MCP 실측(해당 시간창):**

- Logs Insights(`trend/web-access`) 처리 로그: **3,160건**
- 5xx 로그: **7건** (약 **0.22%**)
- 비고: `trend/web-access`는 Caddy 경유 요청 기준이라 Locust 총 요청과 1:1로 일치하지 않을 수 있음.
- EC2 CPUUtilization: 평균 **31.18%**, 최대 **59.16%**
- EC2 mem_used_percent(CWAgent): 평균 **76.15%**, 최대 **76.31%**
- RDS CPUUtilization: 평균 **13.15%**, 최대 **22.45%**
- RDS ReadIOPS / WriteIOPS: 평균 **7.55 / 5.08**, 최대 **13.15 / 9.67**
- 한 줄 판정: **EC2 메모리 사용률이 높고 RDS 부하도 동반 상승**한 고압 구간.

---

### 일곱 번째 테스트 (2026-03-16 19h49, 동시 1,000명, **Redis 적용 후**)

**리포트:** `Locust_2026-03-16-19h49` (3분 5초, 동시 사용자 **10,000명**, `user_qa_query`는 @task(0)으로 제외). **목록 API에 Redis 캐시 적용 이후** 상태.


| 지표                   | 기준 (목표)                    | 일곱 번째 테스트 결과                                 | 판정                                  |
| -------------------- | -------------------------- | -------------------------------------------- | ----------------------------------- |
| **Failure %**        | 0%                         | **0건 / 62,137건 = 0%**                        | **좋음** — 목표 달성                      |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **약 335.6 RPS** (10,000명 구간)                 | **좋음** — 매우 높은 처리량                  |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **0.86초(857ms)**, 95% **3.2초(3200ms)** | **나쁨** — 목표에는 미달, 5차(2.8초/6초)보다는 개선 |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초         | QA POST는 이번 테스트에서 제외                         | -                                   |
| **Response time 추이** | 부하 올려도 수평 유지               | 10,000명 유지 구간에서 평균 ~0.8초, 95% ~3초대 수준 유지     | **보통** — 3·4·5차보다 안정적이지만 목표보다는 느림   |


**엔드포인트별 통계 (요약, Redis 적용 후):**

- `/api/analyzer/analysis-results/?page_size=10`  
  - Requests: 13,351, Failures: 0, Avg: 841.78ms, 95%: 3200ms, RPS: 72.11
- `/api/analyzer/analysis/keywords/`  
  - Requests: 10,922, Failures: 0, Avg: 841.79ms, 95%: 3100ms, RPS: 58.99
- `/api/dashboard/news/?page_size=20`  
  - Requests: 13,463, Failures: 0, Avg: 849.65ms, 95%: 3100ms, RPS: 72.71
- `/api/dashboard/social/?page_size=20`  
  - Requests: 13,455, Failures: 0, Avg: 854.56ms, 95%: 3200ms, RPS: 72.67
- `/api/user_qa/history/?page_size=10`  
  - Requests: 10,946, Failures: 0, Avg: 905.8ms, 95%: 3300ms, RPS: 59.12

**요약:**

- **Redis 적용 후** 동시 10,000명에서 **실패율 0%**, 총 RPS **약 335.6**으로 매우 높은 처리량을 유지했다.  
- Redis 캐시·analysis-results 최적화로 6차(Redis 적용 전) 대비 **평균 21.8초 → 0.86초, 95% 47초 → 3.2초**로 응답 시간이 크게 개선되었다.  
- 다만 프로젝트 목표(일반 API 평균 < 200ms, 95% < 500ms) 관점에서는 여전히 **목표 미달**이며, 99% 지연이 ~7초 수준으로 꼬리 지연도 남아 있다.

**CloudWatch MCP 실측(해당 시간창):**

- Logs Insights(`trend/web-access`) 처리 로그: **50,257건**
- 5xx 로그: **18건** (약 **0.04%**)
- 비고: `trend/web-access`는 Caddy 경유 요청 기준이라 Locust 총 요청과 1:1로 일치하지 않을 수 있음.
- EC2 CPUUtilization: 평균 **56.97%**, 최대 **88.20%**
- EC2 mem_used_percent(CWAgent): 평균 **35.08%**, 최대 **38.05%**
- RDS CPUUtilization: 평균 **4.49%**, 최대 **4.78%**
- RDS ReadIOPS / WriteIOPS: 평균 **6.37 / 3.63**, 최대 **11.92 / 9.71**
- 한 줄 판정: **앱(EC2) 중심 부하가 크고 DB(RDS)는 비교적 여유**인 구간.

---

### 여덟 번째 테스트 (2026-03-17 12h58, Redis + gevent, 동시 1,000명)

**리포트:** `Locust_Redis+gevent 설정 후 천명 테스트` (5분 1초, 동시 사용자 **1,000명**, QA POST 제외)


| 지표                   | 기준 (목표)                    | 여덟 번째 테스트 결과                                     | 판정                      |
| -------------------- | -------------------------- | ------------------------------------------------ | ----------------------- |
| **Failure %**        | 0%                         | **2,175건 / 76,020건 ≈ 2.86%**                     | **나쁨** — 연결 끊김 계열 실패 존재 |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **현재 약 261.4 RPS** (총 평균 약 253.0 RPS)            | **좋음** — 처리량은 높음        |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **1.82초(1824ms)**, 95% **4.4초(4400ms)**    | **나쁨** — 목표 미달          |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초         | QA POST 제외, `history` 평균 **1.93초**, 99% **5.0초** | 참고                      |
| **Response time 추이** | 부하 올려도 수평 유지               | 평균 1~2초대, 95% 4초대 구간 유지                          | **보통** — 고부하에서 지연 존재    |


**엔드포인트별 실패 (합산):**

- `/api/dashboard/news/?page_size=20` 491건  
- `/api/dashboard/social/?page_size=20` 480건  
- `/api/analyzer/analysis-results/?page_size=10` 479건  
- `/api/user_qa/history/?page_size=10` 367건  
- `/api/analyzer/analysis/keywords/` 358건

**요약:** Redis + gevent 적용으로 1,000명 구간에서 처리량은 높게 유지(RPS 250+)되었지만, 연결 리셋/연결 종료로 실패율 2.86%가 남아 목표(0%)에는 미달.

**CloudWatch MCP 실측(해당 시간창):**

- Logs Insights(`trend/web-access`) 처리 로그: **12,364건**
- 5xx 로그: **28건** (약 **0.23%**)
- 비고: 이 구간은 실운영 경유 경로/테스트 대상 호스트에 따라 로그 집계가 축소될 수 있음.
- EC2 CPUUtilization: 평균 **45.98%**, 최대 **48.02%**
- EC2 mem_used_percent(CWAgent): 평균 **59.71%**, 최대 **62.14%**
- RDS CPUUtilization: 평균 **4.56%**, 최대 **5.15%**
- RDS ReadIOPS / WriteIOPS: 평균 **4.99 / 2.59**, 최대 **12.21 / 9.64**
- 한 줄 판정: **EC2 자원 사용은 중간 이상, RDS는 여유**로 앱/캐시 계층 최적화 우선.

**왜 7차보다 8차가 나빠졌는가(해석):**
- 8차는 `gevent` 도입으로 동시 처리 잠재력은 확보했지만, 당시 설정(`worker-connections` 과다 등)과 연결 안정성 문제가 함께 나타나 **연결 리셋/종료 실패(2.86%)**가 발생했다.
- `gevent`는 I/O 대기 구간 효율을 높여 **처리량(RPS)** 개선에 유리하지만, N+1/직렬화 비용/집계 쿼리 같은 상위 병목을 직접 해결하지는 못한다.
- 즉 8차는 `gevent` 자체의 실패가 아니라, **도입 초기 튜닝 미완성 + 애플리케이션 병목 잔존**이 겹친 과도기 구간으로 보는 것이 맞다.

---

### 아홉 번째 테스트 (2026-03-26 04h37, Redis + gevent + Caddy + N+1 해결, 동시 1,000명)

**리포트:** `Locust_Redis+gevent+caddy+n+1 해결 후 천명 테스트` (3분, 동시 사용자 **1,000명**, QA POST 제외)


| 지표                   | 기준 (목표)                    | 아홉 번째 테스트 결과                                     | 판정                          |
| -------------------- | -------------------------- | ------------------------------------------------ | --------------------------- |
| **Failure %**        | 0%                         | **1,370건 / 48,493건 ≈ 2.82%**                     | **나쁨** — 8차 대비 소폭 개선, 목표 미달 |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **현재 약 309.4 RPS** (총 평균 약 268.6 RPS)            | **좋음** — 8차 대비 처리량 증가       |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **1.52초(1523ms)**, 95% **3.9초(3900ms)**    | **나쁨** — 목표 미달, 8차 대비 개선    |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초         | QA POST 제외, `history` 평균 **1.65초**, 99% **4.3초** | 참고                          |
| **Response time 추이** | 부하 올려도 수평 유지               | 평균 1초대 중반, 95% 3~4초대                             | **보통** — 8차 대비 완화           |


**엔드포인트별 실패 (합산):**

- `/api/analyzer/analysis-results/?page_size=10` 315건  
- `/api/dashboard/news/?page_size=20` 308건  
- `/api/dashboard/social/?page_size=20` 284건  
- `/api/user_qa/history/?page_size=10` 237건  
- `/api/analyzer/analysis/keywords/` 226건

**8차 vs 9차 요약:** Caddy 경유 + N+1 개선 후, **RPS 261.4 → 309.4**, 평균 응답 **1.82초 → 1.52초**, 95% **4.4초 → 3.9초**로 개선. 실패율은 **2.86% → 2.82%**로 소폭 감소했지만, 목표(0%)에는 아직 미달.

**CloudWatch MCP 실측(해당 시간창):**

- Logs Insights(`trend/web-access`) 처리 로그: **45,069건**
- 5xx 로그: **13건** (약 **0.03%**)
- 비고: 9차는 리포트 시각 `04:37`을 **종료 시각**으로 보고, `04:34~04:37 UTC`(3분)로 재매핑.
- EC2 CPUUtilization: 평균 **66.54%**, 최대 **99.99%**
- EC2 mem_used_percent(CWAgent): 평균 **23.48%**, 최대 **23.85%**
- RDS CPUUtilization: 평균 **4.26%**, 최대 **5.87%**
- RDS ReadIOPS / WriteIOPS: 평균 **3.16 / 3.51**, 최대 **8.11 / 9.65**
- 한 줄 판정: **9차도 앱(EC2) CPU 중심 부하가 크고 DB는 여유**인 구간.

---

### 아홉 번째-1 테스트 (2026-03-26 15h33, Redis + gevent + Caddy + N+1 해결(재측정), 동시 1,000명)

**리포트:** `Locust_Redis+gevent+caddy+n+1 해결 후 천명 테스트(2)` (3분, 동시 사용자 **1,000명**, QA POST 제외)


| 지표                   | 기준 (목표)                    | 아홉 번째-1 테스트 결과                                   | 판정                           |
| -------------------- | -------------------------- | ------------------------------------------------ | ---------------------------- |
| **Failure %**        | 0%                         | **1,098건 / 56,089건 ≈ 1.96%**                     | **나쁨** — 9차 대비 개선, 목표(0%) 미달 |
| **RPS**              | 부하 대비 유지, 50명 기준 10 RPS 이상 | **현재 약 328.9 RPS** (총 평균 약 310.3 RPS)            | **좋음** — 9차와 유사 수준           |
| **일반 API 평균/95%**    | < 200ms / < 500ms          | 전체 평균 **1.05초(1048ms)**, 95% **2.6초(2600ms)**    | **나쁨** — 목표 미달, 9차 대비 개선     |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초         | QA POST 제외, `history` 평균 **1.08초**, 99% **2.9초** | 참고                           |
| **Response time 추이** | 부하 올려도 수평 유지               | 평균 1초대 초반, 95% 2~3초대                             | **보통** — 9차 대비 완화            |


**엔드포인트별 실패 (합산):**

- `/api/dashboard/social/?page_size=20` 231건  
- `/api/analyzer/analysis-results/?page_size=10` 230건  
- `/api/dashboard/news/?page_size=20` 230건  
- `/api/user_qa/history/?page_size=10` 211건  
- `/api/analyzer/analysis/keywords/` 196건

**9차 vs 9-1 요약:** 리포트 `Locust_Redis+gevent+caddy+n+1 해결 후 천명 테스트(2).html` 기준. **실패율 2.82% → 1.96%**, **현재 RPS 309.4 → 328.9**, **총 평균 RPS 268.6 → 310.3**, 평균 응답 **1.52초 → 1.05초**, 95% **3.9초 → 2.6초**로 개선. 목표(실패 0%, 일반 API 200ms/500ms)에는 여전히 미달.

**CloudWatch MCP 실측(해당 시간창):**

- Logs Insights(`trend/web-access`) 처리 로그: **42,431건**
- 5xx 로그: **18건** (약 **0.04%**)
- 비고: `trend/web-access`는 Caddy 경유 요청 기준이며, API 직결 테스트는 별도 로그(컨테이너/앱 로그) 확인 필요.
- EC2 CPUUtilization: 평균 **49.35%**, 최대 **99.99%**
- EC2 mem_used_percent(CWAgent): 평균 **46.29%**, 최대 **46.94%**
- RDS CPUUtilization: 평균 **3.41%**, 최대 **3.62%**
- RDS ReadIOPS / WriteIOPS: 평균 **5.28 / 3.67**, 최대 **11.58 / 9.55**
- 한 줄 판정: **EC2 CPU 순간 포화(최대 99.99%) 징후**가 보이며 DB는 여유 상태.

---

### 열 번째 테스트 (2026-03-27 14h28 KST, Redis + gevent + Caddy + N+1 + **Gunicorn 안정화 옵션**, 동시 1,000명)

**리포트:** `Locust_Redis+gevent+caddy+n+1+Gunicorn설정추가 후 천명 테스트.html` (3분, 동시 사용자 **1,000명**, QA POST 제외). **UTC** `2026-03-27T05:28:21Z` ~ `05:31:21Z` (KST 14:28:21 ~ 14:31:21).


| 지표                   | 기준 (목표)            | 열 번째 테스트 결과                                     | 판정                                             |
| -------------------- | ------------------ | ----------------------------------------------- | ---------------------------------------------- |
| **Failure %**        | 0%                 | **1건 / 63,894건 ≈ 0.0016%** (ConnectionReset 1건) | **좋음** — 사실상 0%에 근접                            |
| **RPS**              | 부하 대비 유지           | **현재 약 354.6 RPS** (총 평균과 동일)                   | **좋음** — 9-1차(328.9) 대비 상승                     |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **0.68초(681ms)**, 95% **1.9초(1900ms)**    | **나쁨** — 목표 미달, **9-1(1048ms / 2600ms) 대비 개선** |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST 제외, `history` 평균 **0.71초**, 99% **11초** | 참고                                             |
| **Response time 추이** | 부하 올려도 수평 유지       | **중간 구간(약 1분) p95 10~13초·RPS 급감** 후 완만히 회복      | **보통** — 전체 평균은 개선, **스파이크 구간 존재**             |


**엔드포인트별 통계 (요약):**

- `/api/analyzer/analysis-results/?page_size=10` — Requests: 14,003, Failures: **1**, Avg: **642ms**, 95%: 1900ms, 99%: 10000ms, RPS: 77.72  
- `/api/analyzer/analysis/keywords/` — 11,026 / 0 / **682ms** / 1900ms / 11000ms / 61.19  
- `/api/dashboard/news/?page_size=20` — 13,790 / 0 / **675ms** / 1900ms / 11000ms / 76.53  
- `/api/dashboard/social/?page_size=20` — 13,956 / 0 / **698ms** / 1900ms / 11000ms / 77.45  
- `/api/user_qa/history/?page_size=10` — 11,119 / 0 / **714ms** / 2100ms / 11000ms / 61.71

**9-1 vs 10차 요약:** Gunicorn(`--graceful-timeout`, `--keep-alive`, `--max-requests` 등) 반영 후 **평균 1048ms → 681ms**, **95% 2600ms → 1900ms**, **실패 1.96% → 0.0016%**, **RPS 328.9 → 354.6**으로 전반적 개선. 다만 리포트 타임라인상 **05:29:17~05:29:42 UTC** 부근에 **p95 최대 약 13초·처리 RPS 137~230 수준으로 급락**하는 구간이 있어, 평균은 좋아도 **꼬리·스파이크**는 남음.

**중간 스파이크에 대한 해석 (Locust `history` + CloudWatch 교차):**

- 동일 구간에서 **전역 평균 응답 시간**이 약 **300ms대 → 860ms 피크**까지 올라갔다가 이후 **600ms대**로 완만히 수렴하는 패턴이 보임.  
- **p95**는 해당 구간에서 **약 10~13초**까지 튀고, **동시 처리 RPS**는 **420 전후 → 130~230**으로 떨어짐 → **큐 적체·CPU 포화**에 가까운 형태.  
- CloudWatch(EC2 `CPUUtilization`, 분 단위)에서는 **14:30·14:31 KST(=05:30·05:31 UTC)** 에 **약 99.8~99.9%** 로 찍히며, Locust 스파이크(05:29대) 직후 **지속 고부하**와 맞물림.  
- RDS `trend-db` CPU는 동일 창에서 **대략 3~5%** 수준, **ReadIOPS** 만 분 단위로 **한때 ~12.7** 까지 오름 → **병목은 EC2(앱/프록시/워커) 쪽**, DB는 상대적으로 여유.  
- **원인 후보 (단일 EC2·1000명·캐시 미스·워커 재시작 교차 등):**  
  - **EC2 vCPU 포화**로 Gunicorn gevent 워커·Caddy가 동시에 처리할 여력이 줄어 **대기열 길이 증가**  
  - **캐시 콜드 미스**가 한꺼번에 몰리면 DB ReadIOPS·앱 CPU가 동시에 튐  
  - `**--max-requests` 도달 시 워커 순차 재시작**이 스파이크 시각과 겹치면 일시적 처리량 하락 가능  
  - **Celery/임베딩** 등 동일 호스트 작업이 겹치면 CPU 경쟁

**추가 개선 방향:**

- 스파이크 구간을 **1분 해상도 이상**으로 좁혀(메트릭·컨테이너 로그) **워커 재시작·Celery·캐시 미스** 여부를 분리 확인.  
- **목록 캐시 TTL** (`LIST_API_CACHE_TTL_DASHBOARD` / `LIST_API_CACHE_TTL_ANALYZER` / `LIST_API_CACHE_TTL_QA_HISTORY`, 또는 통일 시 `LIST_API_CACHE_TTL`)·**워커 수**·`**max-requests`** 는 한 번에 하나씩 바꿔 Locust 재측정(원인 분리).  
- 목표가 **p95 < 500ms** 이면 **단일 인스턴스 한계**에 가까우므로, 남는 여지는 **캐시 히트율·쿼리·응답 크기** 위주.

**CloudWatch MCP 실측(UTC `05:28:21` ~ `05:31:21`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **64,073건** (Locust 총 요청과 1:1은 아닐 수 있음)  
- `500`/`502`/`503` 패턴 스캔: **0건** (Caddy 로그 포맷 한계 가능)  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`, 분 단위 평균): 약 **3.5% → 73.2% → 99.83% → 99.91%** (구간 평균 **약 69%**, 최대 **약 99.9%**)  
- EC2 `mem_used_percent`(CWAgent): 평균 **32.95%**, 최대 **33.40%**  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`)  
- RDS `trend-db` `CPUUtilization`: 분당 약 **3.8% ~ 5.2%** (구간 평균 **약 4.3%**)  
- RDS ReadIOPS / WriteIOPS (평균, 분 단위): 대략 **2.4~~12.7 / 0.6~~9.3** (스파이크 분에 Read·Write 함께 상승)  
- 세 줄 판정:
  1. **EC2 CPU:** 분 단위 최대치가 99%대까지 올라 스파이크 시점의 처리량 급락과 정합성이 높다(앱/프록시 계층 병목 우선).
  2. **EC2 메모리:** `mem_used_percent`는 33% 내외로 여유가 있어, 이번 구간의 1차 병목은 메모리보다는 CPU/큐 적체 가능성이 크다.
  3. **RDS/안정성:** RDS CPU는 3~5%로 낮고 실패도 1건 수준이라, DB 포화보다는 호스트 자원 경쟁/일시적 워커 상태 변화 영향이 더 유력하다.

---

### 열 번째-1 테스트 (2026-03-27 14h34 KST, 동 설정 재측정, 동시 1,000명)

**리포트:** `Locust_Redis+gevent+caddy+n+1+Gunicorn설정추가 후 천명 테스트(2).html` (3분 1초, 동시 **1,000명**, QA POST 제외). **UTC** `2026-03-27T05:34:47Z` ~ `05:37:48Z`.


| 지표                   | 기준 (목표)            | 열 번째-1 테스트 결과                                                                          | 판정                                             |
| -------------------- | ------------------ | -------------------------------------------------------------------------------------- | ---------------------------------------------- |
| **Failure %**        | 0%                 | **0건 / 65,476건 = 0%**                                                                  | **좋음** — 목표 달성                                 |
| **RPS**              | 부하 대비 유지           | **현재 약 362.3 RPS**                                                                     | **좋음** — 10차·9-1 대비 최고 수준                      |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **0.61초(612ms)**, 95% **2.0초**                                                   | **나쁨** — 목표 미달, **10차(681ms/1900ms) 대비 추가 개선** |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST 제외, `history` 평균 **0.67초**, 99% **4.2초**                                       | 참고 — 99% 꼬리는 10차보다 양호                          |
| **Response time 추이** | 부하 올려도 수평 유지       | **05:35:29~05:35:34 UTC**, **05:36:39~05:36:44 UTC** 부근 **p95 2.3초·13초** 등 **중간 스파이크** | **보통** — 평균·99%는 개선, **간헐적 꼬리** 잔존             |


**엔드포인트별 통계 (요약):**

- `/api/analyzer/analysis-results/?page_size=10` — 14,286 / 0 / **580ms** / 2000ms / 3000ms / 79.05  
- `/api/analyzer/analysis/keywords/` — 11,397 / 0 / **597ms** / 2000ms / 3100ms / 63.06  
- `/api/dashboard/news/?page_size=20` — 14,300 / 0 / **610ms** / 2000ms / 3200ms / 79.12  
- `/api/dashboard/social/?page_size=20` — 14,111 / 0 / **609ms** / 2000ms / 2900ms / 78.08  
- `/api/user_qa/history/?page_size=10` — 11,382 / 0 / **671ms** / 2100ms / 4200ms / 62.98

**10차 vs 10-1 요약:** 재측정에서 **실패 0건**, **평균 681ms → 612ms**, **95% 1900ms → 2000ms**(소폭 악화), **99% 11000ms → 3100ms**(꼬리 대폭 개선), **RPS 354.6 → 362.3**. 중간 스파이크는 여전하나 **극단 꼬리(99%)** 는 완화된 편.

**CloudWatch MCP 실측(UTC `05:34:47` ~ `05:37:48`, `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **65,769건**  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`): 분당 약 **3.3% → 31.2% → 99.81% → 99.54%** (구간 평균 **약 58%**, 최대 **약 99.8%**)  
- EC2 `mem_used_percent`(CWAgent): 평균 **32.20%**, 최대 **32.76%**  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`)  
- RDS `trend-db` `CPUUtilization`: 분당 약 **3.5% ~ 5.0%** (구간 평균 **약 4.0%**)  
- RDS ReadIOPS / WriteIOPS (분 평균): 대략 **2.7~~13.1 / 0.4~~9.5**  
- 세 줄 판정:
  1. **EC2 CPU:** 10-1차도 분 단위 최대치가 99%대라, 간헐 스파이크 구간의 꼬리 지연 원인으로 CPU 포화 징후가 반복된다.
  2. **EC2 메모리:** `mem_used_percent` 32%대(평균 32.20%, 최대 32.76%)로 안정적이며, 메모리 압박 신호는 관측되지 않는다.
  3. **RDS/안정성:** RDS는 저부하(약 4%대)이고 실패 0건을 유지해, 스파이크의 주 원인은 DB보다 앱 계층의 순간 처리 한계로 판단된다.

---

### 열한 번째 테스트 (2026-03-27 15h23 KST, Gunicorn `max-requests`/`jitter` 제거, 동시 1,000명)

**리포트:** `Locust_Redis+gevent+caddy+n+1+Gunicorn설정추가+maxrequests삭제 후 천명 테스트.html` (3분 1초, 동시 **1,000명**, QA POST 제외). **UTC** `2026-03-27T06:23:27Z` ~ `06:26:28Z`.


| 지표                   | 기준 (목표)            | 열한 번째 테스트 결과                                         | 판정                              |
| -------------------- | ------------------ | ---------------------------------------------------- | ------------------------------- |
| **Failure %**        | 0%                 | **0건 / 70,431건 = 0%**                                | **좋음** — 목표 달성                  |
| **RPS**              | 부하 대비 유지           | **현재 약 390.1 RPS**                                   | **좋음** — 10-1차(362.3) 대비 상승     |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **0.43초(431ms)**, 95% **1.3초(1300ms)**         | **보통~나쁨** — 목표 미달이나 10-1차 대비 개선 |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST 제외, `history` 평균 **0.45초**, 99% **1.7초**     | 참고                              |
| **Response time 추이** | 부하 올려도 수평 유지       | 초반 상승 후 **0.4초대 중심으로 비교적 평탄**, 10차의 10~13초 급스파이크 미관측 | **보통** — 스파이크 완화                |


**엔드포인트별 통계 (요약):**

- `/api/analyzer/analysis-results/?page_size=10` — 15,171 / 0 / **420ms** / 1300ms / 1700ms / 84.04  
- `/api/analyzer/analysis/keywords/` — 12,068 / 0 / **417ms** / 1300ms / 1700ms / 66.85  
- `/api/dashboard/news/?page_size=20` — 15,502 / 0 / **429ms** / 1300ms / 1700ms / 85.87  
- `/api/dashboard/social/?page_size=20` — 15,613 / 0 / **442ms** / 1300ms / 1700ms / 86.48  
- `/api/user_qa/history/?page_size=10` — 12,077 / 0 / **450ms** / 1300ms / 1700ms / 66.90

**10-1 vs 11차 요약:** `max-requests`/`jitter` 제거 상태에서 **실패 0% 유지**, **평균 612ms -> 431ms**, **95% 2000ms -> 1300ms**, **99% 3100ms -> 1700ms**, **RPS 362.3 -> 390.1**로 개선. 관찰된 패턴상 10차 계열의 중간 급스파이크가 줄고 곡선이 완만해짐.

**CloudWatch MCP 실측(UTC `06:23:27` ~ `06:26:28`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **70,537건**  
- `500`/`502`/`503` 패턴 스캔: **0건**  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`): 약 **5.79% -> 62.76% -> 100.00%** (구간 평균 **약 56.19%**, 최대 **100.00%**)  
- EC2 `mem_used_percent`(CWAgent): 평균 **23.33%**, 최대 **24.74%**  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`)  
- RDS `trend-db` `CPUUtilization`: 평균 **약 3.97%**, 최대 **약 4.14%**  
- RDS ReadIOPS / WriteIOPS (평균, 분 단위): **약 6.86 / 4.10**, 최대 **약 12.87 / 9.37**  
- 세 줄 판정:
  1. **EC2 CPU:** 분 단위 최대 100%가 관측되지만, 애플리케이션 지연 지표(p95/p99)는 이전(10-1) 대비 개선되어 CPU 피크가 즉시 장애로 이어지진 않았다.
  2. **EC2 메모리:** `mem_used_percent` 20%대 중반으로 여유 구간이며, 메모리 압박 징후는 없다.
  3. **RDS/안정성:** RDS는 저부하, 5xx 0건/실패 0건으로 안정적이며, 병목 우선순위는 DB보다 앱 계층 튜닝이 타당하다.

---

### 열한 번째-1 테스트 (2026-03-27 15h28 KST, 동 설정 재측정, 동시 1,000명)

**리포트:** `Locust_Redis+gevent+caddy+n+1+Gunicorn설정추가+maxrequests삭제 후 천명 테스트(2).html` (3분 1초, 동시 **1,000명**, QA POST 제외). **UTC** `2026-03-27T06:27:58Z` ~ `06:30:59Z`.


| 지표                   | 기준 (목표)            | 열한 번째-1 테스트 결과                                    | 판정                             |
| -------------------- | ------------------ | ------------------------------------------------- | ------------------------------ |
| **Failure %**        | 0%                 | **0건 / 69,626건 = 0%**                             | **좋음** — 목표 달성                 |
| **RPS**              | 부하 대비 유지           | **현재 약 385.6 RPS**                                | **좋음** — 10-1차 대비 상승, 11차와 유사  |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **0.46초(462ms)**, 95% **1.5초(1500ms)**      | **보통~나쁨** — 목표 미달, 10-1차 대비 개선 |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST 제외, `history` 평균 **0.48초**, 99% **2.0초**  | 참고                             |
| **Response time 추이** | 부하 올려도 수평 유지       | 11차보다 약간 높지만 **0.4~0.5초대 중심으로 안정**, 10차급 급스파이크 없음 | **보통** — 재현성 양호                |


**엔드포인트별 통계 (요약):**

- `/api/analyzer/analysis-results/?page_size=10` — 15,120 / 0 / **447ms** / 1400ms / 1900ms / 83.74  
- `/api/analyzer/analysis/keywords/` — 12,053 / 0 / **448ms** / 1400ms / 1900ms / 66.75  
- `/api/dashboard/news/?page_size=20` — 15,141 / 0 / **463ms** / 1500ms / 1900ms / 83.85  
- `/api/dashboard/social/?page_size=20` — 15,248 / 0 / **474ms** / 1500ms / 2000ms / 84.45  
- `/api/user_qa/history/?page_size=10` — 12,064 / 0 / **478ms** / 1500ms / 1900ms / 66.81

**11차 vs 11-1 요약:** 재측정에서도 **실패 0건** 유지. 11차 대비 평균/백분위가 소폭 높아졌지만(431ms/1300ms -> 462ms/1500ms), 10-1 대비는 여전히 개선 범위(612ms/2000ms)이며, 스파이크 패턴은 완화 상태가 재현됨.

**CloudWatch MCP 실측(UTC `06:27:58` ~ `06:30:59`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **69,778건**  
- `500`/`502`/`503` 패턴 스캔: **0건**  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`): 약 **28.93% -> 13.69% -> 98.84%** (구간 평균 **약 47.16%**, 최대 **98.84%**)  
- EC2 `mem_used_percent`(CWAgent): 평균 **24.60%**, 최대 **25.06%**  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`)  
- RDS `trend-db` `CPUUtilization`: 평균 **약 3.42%**, 최대 **약 3.75%**  
- RDS ReadIOPS / WriteIOPS (평균, 분 단위): **약 2.97 / 0.79**, 최대 **약 3.58 / 0.98**  
- 세 줄 판정:
  1. **EC2 CPU:** 최대 98%대 피크는 남아 있으나, 응답지표가 10차 대비 안정화되어 운영상 체감 성능은 개선 구간이다.
  2. **EC2 메모리:** 24~25% 수준으로 여유가 크고, 현재 병목과의 상관성은 낮다.
  3. **RDS/안정성:** RDS 저부하·5xx 0건·실패 0건으로 안정성이 확인되며, 후속 최적화는 앱 계층 p95 추가 절감이 핵심이다.

---

### 열한 번째-2 테스트 (k6, 2026-03-27 16h30 KST, `constant-arrival-rate`, RATE=1000)

**리포트:** `k6 run -e BASE_URL=http://3.37.203.226:8000 -e RATE=1000 -e DURATION=3m k6_trend_api.js`  
실행 결과 요약: `http_reqs=74,946`, `http_req_failed=0.00%`, `http_req_duration p(95)=1.32s`, `avg=410.8ms`, `max=9.85s`, `dropped_iterations=105,055`.


| 지표                   | 기준 (목표)            | 열한 번째-2 테스트 결과                             | 판정                             |
| -------------------- | ------------------ | ------------------------------------------ | ------------------------------ |
| **Failure %**        | 0%                 | **0건 / 74,946건 = 0%**                      | **좋음** — 목표 달성                 |
| **RPS(TPS)**         | 부하 대비 유지           | **약 408.8 RPS(TPS)**                       | **좋음** — 11/11-1 대비 처리량 증가     |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **0.41초(410.8ms)**, 95% **1.32초**    | **보통~나쁨** — 평균은 개선, p95는 목표 미달 |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST 제외(`user_qa_query` 가중치 0)          | 참고                             |
| **Response time 추이** | 부하 올려도 수평 유지       | 중앙값 **242ms**, p95 **1.32s**, 최대 **9.85s** | **보통** — 꼬리 지연은 남음             |


**k6 추가 해석 (중요):**

- `WARN Insufficient VUs` + `dropped_iterations 105,055`가 관측됨.  
- 의미: `RATE=1000`(초당 1000 iteration 시작) 목표를 **부하 발생기(VU 한도 1000)** 가 완전히 따라가지 못해, 실제 처리량은 약 **408.8 RPS(TPS)** 로 제한됨.  
- 따라서 이번 11-2 결과는 "서비스 한계"와 함께 "부하 생성기 설정 한계(maxVUs/preAllocatedVUs)"가 섞인 결과로 해석해야 함.

**엔드포인트별 참고:**

- 현재 k6 스크립트는 endpoint 태그를 붙여 요청하지만, 이번 실행 출력은 `summaryTrendStats` 기본 집계만 표시되어 엔드포인트별 수치는 별도 요약 옵션 추가 시 확인 가능.

**CloudWatch MCP 실측(UTC `07:30:00` ~ `07:33:00`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **67,916건**  
(k6 총 74,946건과 차이: `:8000` 직결/로그 경로 차이 및 시간 경계 포함 가능)  
- `500`/`502`/`503` 패턴 스캔: **0건**  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`): 약 **3.19% -> 90.58% -> 99.97%** (구간 평균 **약 64.58%**, 최대 **99.97%**)  
- EC2 `mem_used_percent`(CWAgent): 평균 **51.02%**, 최대 **51.30%**  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`)  
- RDS `trend-db` `CPUUtilization`: 평균 **약 3.90%**, 최대 **약 4.47%**  
- RDS ReadIOPS / WriteIOPS (평균, 분 단위): **약 6.47 / 3.51**, 최대 **약 11.91 / 9.35**  
- 세 줄 판정:
  1. **EC2 CPU:** 분 단위 최대 99.97%로 앱 계층 CPU 피크가 반복되어 p95 꼬리 지연에 직접 영향을 준다.
  2. **EC2 메모리:** 51%대 사용률로 여유가 남아, 이번 구간의 주 병목은 메모리보다 CPU/동시 처리 한계다.
  3. **RDS/안정성:** RDS는 저부하(약 4% 이하)·5xx 0건으로 안정적이며, 우선 최적화 대상은 DB보다 앱/워커/부하생성 설정이다.

---

### 열한 번째-3 테스트 (k6, 2026-03-27 17h16 KST, `constant-arrival-rate`, RATE=400)

**리포트:** `k6 run -e BASE_URL=http://3.37.203.226:8000 -e RATE=400 -e DURATION=3m -e PRE_VUS=600 -e MAX_VUS=1800 k6_trend_api.js`  
실행 결과 요약: `http_reqs=71,673`, `http_req_failed=0.00%`, `http_req_duration p(95)=308.76ms`, `avg=76.4ms`, `max=3.2s`, `dropped_iterations=328`.


| 지표                   | 기준 (목표)            | 열한 번째-3 테스트 결과                                 | 판정                                      |
| -------------------- | ------------------ | ---------------------------------------------- | --------------------------------------- |
| **Failure %**        | 0%                 | **0건 / 71,673건 = 0%**                          | **좋음** — 목표 달성                          |
| **RPS(TPS)**         | 부하 대비 유지           | **약 391.9 RPS(TPS)**                           | **좋음** — 11차(Locust)와 유사한 처리량을 안정적으로 재현 |
| **일반 API 평균/95%**    | < 200ms / < 500ms  | 전체 평균 **76.4ms**, 95% **308.76ms**             | **좋음** — 목표 구간 만족                       |
| **무거운 API (QA)**     | 평균 < 5초, 99% < 15초 | QA POST 제외(`user_qa_query` 가중치 0)              | 참고                                      |
| **Response time 추이** | 부하 올려도 수평 유지       | 중앙값 **35.11ms**, p95 **308.76ms**, 최대 **3.2s** | **좋음** — 꼬리 지연은 제한적                     |


**k6 추가 해석 (중요):**

- 임계치(`http_req_failed`, `http_req_duration`)를 모두 통과했고 실패율 0%를 유지했다.  
- `dropped_iterations=328`은 전체 대비 약 **0.46%**로 매우 낮아, 부하 생성기 한계 영향이 작다.  
- 동일 스크립트의 RATE=600 대비, RATE=400은 지연/실패가 급격히 낮아져 **안정 운용 가능한 구간**으로 해석된다.

**CloudWatch MCP 실측(UTC `08:16:00` ~ `08:19:00`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **66,870건**  
(k6 총 71,673건과 차이: 직결 경로/로그 경계 시점 차이 영향 가능)  
- `500`/`502`/`503` 패턴 스캔: **17건** (약 **0.025%**)  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`): 약 **15.44% -> 91.33% -> 90.97%** (구간 평균 **약 65.91%**, 최대 **91.33%**)  
- EC2 `mem_used_percent`(CWAgent): 평균 **48.23%**, 최대 **48.64%**  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`)  
- RDS `trend-db` `CPUUtilization`: 평균 **약 3.71%**, 최대 **약 3.87%**  
- RDS ReadIOPS / WriteIOPS (평균, 분 단위): **약 4.98 / 0.59**, 최대 **약 6.52 / 0.65**  
- 세 줄 판정:
  1. **EC2 CPU:** 분 단위 최대 91%까지 상승하지만, 애플리케이션 지표(p95 309ms, 실패 0%)는 안정적이라 현재 RATE=400은 실운영 가능한 구간이다.
  2. **EC2 메모리:** 48%대 사용률로 메모리 압박은 없고, 병목 우선순위는 메모리보다 CPU/동시성 튜닝이다.
  3. **RDS/안정성:** RDS는 저부하(약 4% 이하)이며 전체 실패율 0%를 유지해, 현 구간의 핵심 제약은 DB보다 앱 계층 상단 처리량에 가깝다.

---

### 열두 번째-1 테스트 (2026-03-29 22h29 KST, Locust, 동시 **1,000명**, **3분**, QA POST 제외)

**목적:** 문서상 **12차(10분)** 와 **11-1(3분)** 만 놓고 숫자를 비교하면 기간이 달라 왜곡된다는 지적이 있어, **열한 번째-1과 동일하게 3분**으로 다시 측정한 결과다.

**리포트:** `Locust_Redis+gevent+caddy+n+1+Gunicorn설정추가+maxrequests삭제+defer,경량serializer 설정 후 천명 테스트(2).html`  
**호스트:** `http://3.37.203.226:8000` · **Locust 파일:** `locustfile.py`  
**구간:** **UTC** `2026-03-29T13:29:46Z` ~ `2026-03-29T13:32:46Z` (KST 22:29:46 ~ 22:32:46, **3분**).

**코드·설정 (측정 시점):** Redis + gevent + Caddy + N+1·Gunicorn 안정화 + `max-requests` 제거 + `**defer`·목록 경량 Serializer** 반영. `feat(cache)` 목록 TTL 그룹 분리(`LIST_API_CACHE_TTL_*`) 및 서버 `.env` TTL(예: 7·9·3초 등)은 배포 상태에 따름.

**해석:** **3분 창을 11-1과 맞춘 뒤**에도 평균·95%·RPS가 **11-1(평균 ~462ms, 95% ~1500ms, RPS ~385)** 대비 크게 개선되었으므로, **원인 후보 1순위는 `defer` + 경량 Serializer(및 그에 수반된 DB·직렬화 부담 감소)** 로 보는 것이 타당하다.


| 지표                    | 기준 (목표)            | 열두 번째-1 결과                                                   | 판정             |
| --------------------- | ------------------ | ------------------------------------------------------------ | -------------- |
| **Failure %**         | 0%                 | **0건 / 83,039건 = 0%**                                        | **좋음** — 목표 달성 |
| **RPS**               | 부하 대비 유지           | **약 460.7 RPS** (Aggregated)                                 | **좋음**         |
| **일반 API 평균/95%/99%** | < 200ms / < 500ms  | 전체 평균 **63.3ms**, **50% 27ms**, **95% 260ms**, **99% 650ms** | **좋음**         |
| **무거운 API (QA)**      | 평균 < 5초, 99% < 15초 | QA POST 제외(`user_qa_query` @task(0))                         | -              |
| **Response time 추이**  | 부하 구간 안정           | 3분·실패 0%                                                     | **좋음**         |


**11-1 vs 12-1 (동일 3분·직접 비교 가능):**


| 구분               | 열한 번째-1       | 열두 번째-1    |
| ---------------- | ------------- | ---------- |
| 기간               | 3분            | 3분         |
| 평균 응답            | ~**462ms**    | **63.3ms** |
| 95%              | ~**1500ms**   | **260ms**  |
| RPS (Aggregated) | ~**385**      | **~461**   |
| 실패               | (문서 기준 0% 목표) | **0%**     |


**엔드포인트별 통계 (요약):**

- `/api/analyzer/analysis-results/?page_size=10` — 17,809 / 0 / 평균 **56ms**, 50% **17ms**, 95% **250ms**, 99% **630ms**, **98.8 RPS**  
- `/api/analyzer/analysis/keywords/` — 14,133 / 0 / 평균 **55ms**, 50% **17ms**, 95% **240ms**, 99% **620ms**, **78.4 RPS**  
- `/api/dashboard/news/?page_size=20` — 18,215 / 0 / 평균 **59ms**, 50% **18ms**, 95% **260ms**, 99% **660ms**, **101.1 RPS**  
- `/api/dashboard/social/?page_size=20` — 18,424 / 0 / 평균 **76ms**, 50% **36ms**, 95% **270ms**, 99% **650ms**, **102.2 RPS**  
- `/api/user_qa/history/?page_size=10` — 14,458 / 0 / 평균 **69ms**, 50% **27ms**, 95% **290ms**, 99% **690ms**, **80.2 RPS**

**CloudWatch MCP 실측(UTC `2026-03-29T13:29:46Z` ~ `2026-03-29T13:32:46Z`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **82,943건** (Locust 총 83,039건과 **약 96건 차이**; Caddy 경유·샘플링·경계 시각 차이로 볼 수 있음)  
- `@message`에 `500` / `502` / `503` 단순 패턴 스캔: **0건** (로그 포맷에 따라 누락 가능)  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`, 분 단위 **Average**): 약 **3.4% → 30.2% → 84.2%** (KST 22:29 ~ 22:31 각 분; 구간 단순 평균 **약 39%** 내외, 마지막 분 **약 84%**)  
(MCP 실측 차원: `InstanceId=i-0a66c20698b2f3878`, `InstanceType=m7i-flex.large`)  
- EC2 `mem_used_percent`(CWAgent, `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`): 약 **30.91% → 31.58% → 31.86%** (구간 평균 약 **31.45%**)  
- RDS `trend-db` `CPUUtilization` (분 **Average**): 약 **4.3% → 7.9% → 7.4%** (구간 평균 **약 6.5%** 내외)  
- RDS `ReadIOPS` / `WriteIOPS` (분 **Average**): Read 대략 **2.3 → 14.2 → 5.5**, Write 대략 **0.95 → 10.9 → 1.05**  
- 세 줄 판정:
  1. **EC2 CPU:** 3분 부하 후반 **분당 평균 ~84%** 까지 올라가도 Locust **평균 63ms·95% 260ms·실패 0%** 와 함께 **앱 층 처리량·응답**이 양호한 편.
  2. **EC2 메모리:** 같은 창에서 **약 31%대(평균 31.45%)**로 안정적이며, 메모리 병목 신호는 보이지 않음.
  3. **RDS:** CPU·IOPS 모두 **저부하**로 보이며, `defer`·경량 serializer로 **읽기·직렬화 부담이 줄었다**는 해석과 정합.

**RDS Performance Insights (Database Insights, MCP `aws pi describe-dimension-keys`):** 인스턴스 `trend-db` (`DbiResourceId` `db-HCZWTKDYYGXEPASZFN7SY6RN2I`, PostgreSQL, PI 활성). 메트릭 `**db.load.avg`**, 그룹 `**db.sql**`. `Total` 은 해당 구간 **평균 활성 세션(AAS) 기여** 근사.

**조회 구간 (초기 10분 Locust = 말하는 “그때”):** **KST** `2026-03-29` **20:29:32 ~ 20:39:32** (**10분**) = **UTC** `2026-03-29T11:29:32Z` ~ `2026-03-29T11:39:32Z`. (리포트 `Locust_2026-03-29-20h29_...8000.html` 과 같은 부하.) PI는 분 단위로 정렬되면 **KST 20:29 ~ 20:40** 근처로 맞춰질 수 있음. **3분(12-1)보다 창이 길어 SQL·AAS 표본이 더 나오므로**, 아래 SQL 순위는 **이 10분 창만** 사용한다.

**상위 SQL (AAS 기여 큰 순):** 아래 문장은 `**aws pi describe-dimension-keys` 응답의 `db.sql.statement`를 그대로 옮긴 것**이다. **이 4개는 해당 10분 창에서 잘리지 않고 전문이 내려왔다.** (PI는 SQL·창에 따라 중간 잘림이 나올 수 있어, **문자열이 짧게 끊기지 않은 것만** “전문 확정”으로 본다.)

1. `SELECT COUNT(*) AS "__count" FROM "data_collector_newsarticle"` — **~0.0076**
2. `SELECT "data_collector_socialmediapost"."source_id", COUNT("data_collector_socialmediapost"."id") AS "total" FROM "data_collector_socialmediapost" WHERE "data_collector_socialmediapost"."source_id" IN (1, 2, 5) GROUP BY "data_collector_socialmediapost"."source_id"` — **~0.0076**
3. `SELECT COUNT(*) AS "__count" FROM "data_collector_socialmediapost"` — **~0.0015**
4. `SELECT COUNT(*) AS "__count" FROM "analyzer_trendanalysisresult"` — **~0.0015**

**왜 이게 제일 중요한지:** Locust·앱 지표는 “느리다/괜찮다”까지만 주고, **DB 쪽에서 실제로 시간을 쓰는 쿼리**는 Performance Insights가 잡아 준다. **스케일·HA 전에** “무엇을 줄일지”를 정하려면 **상위 SQL → 코드 경로 → 캐시/쿼리/스키마** 순으로 손대는 게 맞다(부하 재돌리기 전 **우선순위**).

**이 목록으로 하기로 한 일 (액션):**

1. **발생 위치 추적:** 위 SQL이 나오는 View/ORM 경로(뉴스·소셜·분석결과 목록, 대시보드 `source_id` 집계)를 코드에서 대응시킨다.
2. `**COUNT(*)` 3종:** pagination/total에 묶인 반복 스캔이면 **목록 Redis TTL·캐시**, 허용 시 **근사치**, 또는 **집계 테이블/카운터**로 호출 수·비용을 줄인다.
3. `**GROUP BY` 집계:** 대시보드용이면 **짧은 TTL 캐시** 또는 **주기적 rollup**으로 DB에서 매번 돌리지 않게 한다.
4. **검증:** 배포 후 **같은 부하(또는 유사 Locust)** + **같은 PI 구간**에서 위 SQL의 **AAS 기여가 내려갔는지** 확인한다.

**DB 쿼리 최적화와의 연결 (요약):**

- `**COUNT(*)` 전체 테이블**: 인덱스만으로 상한을 못 줄이는 유형이 많아 **캐시·근사·별도 집계**가 직결됨.  
- `**source_id IN (1,2,5)` + `GROUP BY`**: **TTL 캐시·rollup** 후보. (이 10분 상위에는 뉴스 목록 **JOIN** 쿼리가 **별도 순위로 잡히지 않았고**, `COUNT`·이 집계가 두드러짐.)

**참고:** **Locust 지표·11-1 대비 공정 비교**는 **12-1(3분)** 수치를 쓰고, **Database Insights(SQL 순위)** 는 위 **초기 10분(KST 20:29:32~20:39:32)** 만 본다. 초기 10분 HTML: `Locust_2026-03-29-20h29_locustfile.py_http___3.37.203.226_8000.html`.

---

### 열세 번째 테스트 (2026-03-30 16h59 KST, Locust, 동시 **1,000명**, **5분**, QA POST 제외)

**리포트:** `Locust_Redis+gevent+caddy+n+1+Gunicorn설정추가+maxrequests삭제+defer,경량serializer+쿼리 최적화 후 천명 테스트.html`  
**호스트:** `http://3.37.203.226:8000` · **Locust 파일:** `locustfile.py`  
**구간:** **UTC** `2026-03-30T07:59:53Z` ~ `2026-03-30T08:04:54Z` (KST 16:59:53 ~ 17:04:54, **5분 1초**).

**지표 요약:**

| 지표 | 기준 (목표) | 열세 번째 테스트 결과 | 판정 |
|------|-------------|-----------------------|------|
| **Failure %** | 0% | **0건 / 143,521건 = 0%** | **좋음** — 목표 달성 |
| **RPS** | 부하 대비 유지 | **약 478.0 RPS** (Aggregated) | **좋음** |
| **일반 API 평균/95%/99%** | < 200ms / < 500ms | 전체 평균 **28.4ms**, **50% 22ms**, **95% 69ms**, **99% 190ms** | **매우 좋음** |
| **무거운 API (QA)** | 평균 < 5초, 99% < 15초 | QA POST 제외(`user_qa_query` @task(0)) | - |
| **Response time 추이** | 부하 구간 안정 | 5분 유지·실패 0% | **좋음** |

**12-1 대비 13차 2~3줄 요약(얼마나 더 개선됐는지):**
- 평균 응답 `63.3ms` -> `28.4ms`로 약 **55.1% 감소**(약 **2.2배** 개선), 95% `260ms` -> `69ms`로 약 **73.5% 감소**(약 **3.8배** 개선)했습니다.
- 99% `650ms` -> `190ms`로 약 **70.8% 감소**(약 **3.4배** 개선)했고, RPS는 `~460.7` -> `~478.0`로 소폭 증가했으며 Failure는 **0%로 동일**합니다.

**엔드포인트별 통계 (요약):**

- `/api/analyzer/analysis-results/?page_size=10` — 31,224 / 0 / 평균 **24ms**, 50% **16ms**, 95% **65ms**, 99% **180ms**, **104.0 RPS**  
- `/api/analyzer/analysis/keywords/` — 24,838 / 0 / 평균 **25ms**, 50% **16ms**, 95% **66ms**, 99% **180ms**, **82.7 RPS**  
- `/api/dashboard/news/?page_size=20` — 31,545 / 0 / 평균 **25ms**, 50% **16ms**, 95% **66ms**, 99% **180ms**, **105.1 RPS**  
- `/api/dashboard/social/?page_size=20` — 31,114 / 0 / 평균 **35ms**, 50% **26ms**, 95% **75ms**, 99% **190ms**, **103.6 RPS**  
- `/api/user_qa/history/?page_size=10` — 24,800 / 0 / 평균 **34ms**, 50% **26ms**, 95% **75ms**, 99% **200ms**, **82.6 RPS**

**CloudWatch MCP 실측(UTC `2026-03-30T07:59:53Z` ~ `2026-03-30T08:04:54Z`, 리전 `ap-northeast-2`):**

- Logs Insights(`trend/web-access`) 처리 로그: **143,443건** (Locust 총 143,521건과 **약 78건 차이**)  
- `@message`에 ` 500 ` / ` 502 ` / ` 503 ` 단순 패턴 스캔: **0건**  
- EC2 `CPUUtilization` (`i-0a66c20698b2f3878`, 분 **Average**): 약 **3.34% → 19.08% → 70.97% → 71.18% → 71.00%**  
  (구간 단순 평균 약 **47%** 내외, 고부하 분당 약 **71%**)  
- EC2 `mem_used_percent`(CWAgent, `InstanceId=i-0a66c20698b2f3878`, `ImageId=ami-0130d8d35bcd2d433`, `InstanceType=m7i-flex.large`): 약 **21.52% → 22.37% → 22.29% → 22.46% → 22.41%** (구간 평균 약 **22.21%**)  
- RDS `trend-db` `CPUUtilization` (분 **Average**): 약 **4.28% → 5.36% → 4.90% → 4.59% → 4.55%** (구간 평균 약 **4.7%**)  
- RDS `ReadIOPS` / `WriteIOPS` (분 **Average**): Read **4.37 → 16.85 → 5.34 → 3.92 → 3.41**, Write **0.97 → 11.39 → 1.12 → 1.12 → 1.35**

**RDS Performance Insights (MCP `aws pi describe-dimension-keys`):**

- 인스턴스: `trend-db` (`db-HCZWTKDYYGXEPASZFN7SY6RN2I`, PostgreSQL, PI 활성)  
- 같은 시간창(UTC `07:59:53` ~ `08:04:54`)에서 메트릭 `db.load.avg`, 그룹 `db.sql` 조회 결과: **`Keys=[]`** (상위 SQL 미노출)  
- 주변 확장 창(UTC `07:55:00` ~ `08:10:00`)으로 넓혀도 `db.sql`은 **`Keys=[]`**

**해석 (DB 쿼리 최적화 필요도):**

1. 이번 13차 창에서는 PI `db.sql` 상위 항목이 비어 있고, RDS CPU도 **약 4~5%대**라 **DB가 병목으로 보이지 않음**.  
2. 따라서 **지금 시점에서 신규 DB 쿼리 최적화(인덱스/쿼리 구조 변경)를 급하게 추가할 필요는 낮음**.  
3. 우선순위는 DB 튜닝보다 **현 캐시/쿼리 최적화 상태 유지 + 회귀 모니터링**이 맞음.  
4. 다만 데이터량이 커지거나 TTL을 크게 낮추면 `COUNT/GROUP BY`가 재부상할 수 있으니, 동일 창 방식으로 PI/CloudWatch를 주기적으로 재확인할 것.

---

### 목표치 (이번 프로젝트)


| 구분                                                                     | 목표치                           | 비고                                         |
| ---------------------------------------------------------------------- | ----------------------------- | ------------------------------------------ |
| **실패율**                                                                | **0%**                        | 429·5xx 없음. Rate limit·타임아웃 설정 조정으로 달성 목표. |
| **일반 API** (health, news, social, analysis-results, keywords, history) | 평균 < 200ms, 95% < 500ms       | 목록·조회 위주.                                  |
| **무거운 API** (POST /api/user_qa/query/)                                 | 평균 < 5초, 99% < 15초            | LLM 호출 포함.                                 |
| **RPS**                                                                | 동시 사용자 50명 기준 초당 10 RPS 이상 유지 | 부하 시 RPS 급감 없음.                            |
| **Response time 그래프**                                                  | 부하 구간에서 수평 유지                 | 계속 상승하지 않음.                                |


- 테스트할 때마다 위 목표치와 실제 값을 비교해 기록해 두면 개선 여부를 확인하기 쉽다.

---

## 6. 주의

- Locust는 **서버(EC2)가 아닌 로컬 PC**에서 실행하는 것을 권장 (같은 서버에서 돌리면 부하 생성이 서버 지표를 왜곡함).
- EC2 보안 그룹에서 **8000 포트**가 Locust를 실행하는 PC IP(또는 0.0.0.0)에 열려 있어야 함.

